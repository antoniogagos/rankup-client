# Sports domain

ACL-normalized sports catalog and schedule (catalog + schedule capabilities).

## Folder map

- `shared/`: shared IDs + enums for sports models.
- `catalog/`: sports list, competitions, seasons, teams.
- `schedule/`: matchdays, matches, events.
