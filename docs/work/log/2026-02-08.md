# Work log - 2026-02-08

## Summary

-   Followed mandatory entry procedure for the active Epic 008 lane (`START-HERE`, `CURRENT`, active epic, latest daily log, `yarn validate`).
-   Established `WP-008-29` to execute strict 1:1 parity with the architect P0 testing recipe.
-   Added canonical execution document: `docs/work/epics/008-p0-testing-parity-1-1.md`.
-   Updated tracking docs to point execution to `WP-008-29` (`AGENTS.md`, `docs/work/CURRENT.md`, `docs/work/epics/008-domain-tournament-boundaries.md`).
-   Executed `WP-008-29` `PR-001` (baseline + tracking unblock): marked PR-001 complete in the execution document and synchronized `CURRENT` + epic + `AGENTS.md` for the next step (`PR-002`).
-   Executed `WP-008-29` `PR-002` (mapper error mapping parity): added shared `DomainError` type + API problem mapper, exposed mapper-level error mapping helpers, and updated all 9 mapper P0 suites to assert canonical `Problem -> DomainError.kind` outcomes while preserving success DTO->domain assertions.
-   Synced PR-002 completion tracking in `docs/work/epics/008-p0-testing-parity-1-1.md`, `docs/work/CURRENT.md`, `docs/work/epics/008-domain-tournament-boundaries.md`, and `AGENTS.md`, with next execution step set to `PR-003`.
-   Executed `WP-008-29` `PR-003` (contract passthrough parity): added `idempotencyKey` to join/upsert domain params and `ifMatch` to upsert params, forwarded these values through tournament/submissions gateways and SPA HTTP headers, and expanded tournaments/submissions P0 contract tests to assert passthrough explicitly.
-   Synced PR-003 completion tracking in `docs/work/epics/008-p0-testing-parity-1-1.md`, `docs/work/CURRENT.md`, `docs/work/epics/008-domain-tournament-boundaries.md`, and `AGENTS.md`, with next execution step set to `PR-004`.
-   Executed `WP-008-29` `PR-004` (smoke flow parity `ranking -> results`): implemented `scoring/results` domain capability (`models/contracts/services`), wired a dedicated scoring results gateway in composition root + app services, and updated P0 smoke flow #3 to use `tournamentResultsService.getMyMatchdayResults`.
-   Verified smoke flow #3 now hits `/tournaments/{tournamentId}/matchdays/{matchday}/results/me` (`getMyMatchdayResults`) through the real composition root + programmatic api-mock.
-   Synced PR-004 completion tracking in `docs/work/epics/008-p0-testing-parity-1-1.md`, `docs/work/CURRENT.md`, `docs/work/epics/008-domain-tournament-boundaries.md`, and `AGENTS.md`, with next execution step set to `PR-005`.
-   Executed `WP-008-29` `PR-005` (dynamic gateway ratchet parity): replaced static mapper suite checks in `scripts/test-guardrails-p0.ts` with dynamic gateway-pattern scanning (`apps/**/services/api/**/*-gateway.ts` + `packages/**/src/**/gateways/**/*Gateway.ts`) plus deterministic mapper naming rules and a baseline waiver set for pre-ratchet gateways.
-   Preserved `.only/.skip` rejection across all P0 suites and verified ratchet behavior by simulating a new gateway (`apps/rankup-spa/services/api/tournament/ratchet-check-gateway.ts`) without mapper coverage, which failed guardrails with the expected naming guidance.
-   Synced PR-005 completion tracking in `docs/work/epics/008-p0-testing-parity-1-1.md`, `docs/work/CURRENT.md`, `docs/work/epics/008-domain-tournament-boundaries.md`, and `AGENTS.md`, with next execution step set to `PR-006`.
-   Executed `WP-008-29` `PR-006` (determinism + budget gates): added `scripts/test-p0-determinism.ts` (forbidden-call detection + setup checks for `TZ=UTC` and network-off default) and `scripts/test-p0-budget.ts` (P0 run + total/layer budget enforcement from Vitest JSON output).
-   Wired PR-006 gates into execution surfaces: new package scripts (`test:determinism:p0`, `test:budget:p0`, `test:p0:ci`), CI workflow updates (`.github/workflows/test-p0.yml`), and policy docs refresh (`docs/engineering/testing-p0.md`).
-   Verified enforcement with expected-fail probes: temporary P0 file using `Date.now()` failed determinism guard; `P0_BUDGET_TOTAL_MS=1` failed budget gate after successful suite execution.
-   Synced PR-006 completion tracking in `docs/work/epics/008-p0-testing-parity-1-1.md`, `docs/work/CURRENT.md`, `docs/work/epics/008-domain-tournament-boundaries.md`, and `AGENTS.md`, with next execution step set to `PR-007`.
-   Executed `WP-008-29` `PR-007` (global closure + evidence lock): reinforced `scoring` contract parity with explicit `getMyMatchdayResults` success passthrough coverage, then ran and recorded closure commands (`yarn test:guardrails:p0`, `yarn test:p0`, `yarn engine:test`, `yarn validate`) as PASS.
-   Closed `WP-008-29` in the execution plan and Epic 008 tracking with final acceptance criteria A-E fully marked complete; synchronized `AGENTS.md`, `docs/work/CURRENT.md`, active epic, and this daily log.
-   Executed `WP-008-30` (naming drift closure): migrated SPA API gateway folder references from `apps/rankup-spa/services/api/tournament` to `apps/rankup-spa/services/api/tournaments`, including composition-root wiring, cross-slice mappers, P0 mapper suites, and supporting architecture/state docs.
-   Added a dedicated repo guardrail in `scripts/repo-guardrails.ts` to forbid legacy `services/api/tournament` folder/import usage, updated P0 dynamic-gateway baseline paths in `scripts/test-guardrails-p0.ts`, and aligned service-catalog scope naming to canonical `tournaments`.
-   Executed `WP-008-31` (canonical naming closure): renamed remaining singular/plural naming drift across domain/runtime/UI/API files and symbols so tracked content consistently uses `tournament`/`tournaments`.
-   Synced work tracking for `WP-008-31` in `AGENTS.md`, `docs/work/CURRENT.md`, and `docs/work/epics/008-domain-tournament-boundaries.md`, then re-ran full verification gates.
-   Performed a deep ADR-0056 coverage audit against the full OpenAPI catalog (`278` ops / `226` non-admin), confirming current blocking scope remains baseline-only (`39` operations) and documenting the exact gap profile (`missingOwner=32`, fallback mock handlers=`190`).
-   Added the canonical execution plan `docs/work/epics/008-operation-coverage-4way-global.md` (`WP-008-32`) with exact PR ordering, hard ratchet policy for new operations, and measurable A-E acceptance criteria for global 4-way closure.
-   Synced structural tracking for `WP-008-32` planning activation in `AGENTS.md`, `docs/work/CURRENT.md`, and `docs/work/epics/008-domain-tournament-boundaries.md`.
-   Executed `WP-008-32` `PR-001` (tracking + ADR delta + global inventory): added `scripts/repo-operation-coverage-report.ts` + `package.json` command `repo:operation-coverage:report`, generated `diagnostics/operation-coverage-global-report.json`, and updated ADR/quality docs to declare baseline parity transitional toward global 4-way coverage.
-   Synced `PR-001` completion tracking in `docs/work/epics/008-operation-coverage-4way-global.md`, `docs/work/epics/008-domain-tournament-boundaries.md`, `docs/work/CURRENT.md`, and `AGENTS.md`, with active execution moved to `PR-002`.
-   Executed `WP-008-32` `PR-002` (single global gate): replaced split coverage ownership gating in `yarn validate` with `yarn repo:operation-coverage`, updated coverage scripts to run on full non-admin OpenAPI scope (no baseline filter for coverage gate), and added scope-switch readiness in schema/http validators (`OPERATION_COVERAGE_SCOPE` baseline/global).
-   Global ownership diagnostics now run over the full non-admin set and keep `missingOwner` visible as non-blocking debt (`32`) until `PR-004` introduces strict mapping/ownership enforcement.
-   Synced `PR-002` completion tracking in `docs/work/epics/008-operation-coverage-4way-global.md`, `docs/work/epics/008-domain-tournament-boundaries.md`, `docs/work/CURRENT.md`, `AGENTS.md`, ADR 0056, and quality docs; active execution moves to `PR-003`.
-   Executed `WP-008-32` `PR-003` (waivers v2 strict metadata + format migration): hardened `scripts/repo-operation-waivers.ts` to require `owner|reason|issue|createdAt|expiresAt|plan|scope|severity`, reject legacy `expiresOn`, enforce max TTL policy, and reject duplicate `operationId+waiverType`.
-   Updated waiver generation/spec references to v2 format (`scripts/waivers-seed.ts`, `packages/rankup/especificacion-allowlist-operation-waivers.md`, ADR 0056) and kept `diagnostics/operation-waivers.json` canonical (`[]`) after expected-fail schema probe.
-   Synced `PR-003` completion tracking in `docs/work/epics/008-operation-coverage-4way-global.md`, `docs/work/epics/008-domain-tournament-boundaries.md`, `docs/work/CURRENT.md`, `AGENTS.md`, ADR 0056, and quality docs; active execution moves to `PR-004`.
-   Executed `WP-008-32` `PR-004` (central gateway mapping + strict ownership enforcement): added global `gatewayOperationMapping` in `apps/rankup-spa/services/api/gateway-mapping.ts`, backfilled missing account ownership declarations (`auth/me/social/users` gateways), and upgraded coverage/ownership scripts to block on mapping-owner mismatch and missing mapping entries.
-   Verified strict parity closure for PR-004 with full non-admin coverage (`owners=226/226`, `gateway mapping=226/226`) and removed transitional `missingOwner` non-blocking posture.
-   Regenerated `diagnostics/operation-coverage-global-report.json` after PR-004 to keep reproducible inventory aligned with current mapping/ownership state.
-   Synced `PR-004` completion tracking in `docs/work/epics/008-operation-coverage-4way-global.md`, `docs/work/epics/008-domain-tournament-boundaries.md`, `docs/work/CURRENT.md`, `AGENTS.md`, and ADR 0056; active execution moves to `PR-005`.
-   Added `WP-008-33` execution plan (`docs/work/epics/008-problemdetails-domainerror-global.md`) for global ProblemDetails/error taxonomy closure with strict A-E acceptance criteria.
-   Executed `WP-008-33` `PR-001` (runtime canonical mapping): removed generic non-2xx `Error("HTTP ...")` throws from `apps/rankup-spa/services/api/http-client.ts`, routed error paths through `mapProblemToDomainError`, expanded `DomainErrorKind` with engine-aligned kinds + `429/5xx` status mapping, and added dedicated P0 mapper + runtime negative smoke coverage.
-   Added ADR 0069 (`docs/adr/0069-client-problem-details-domain-error-canonicalization.md`) and updated ADR index to formalize canonical runtime/error taxonomy requirements.

## Verification

-   `yarn validate` -> FAIL
-   Notes: `repo:work-log` guardrail failed because `docs/work/log/2026-02-08.md` did not exist yet while structural files were already modified.
-   `yarn validate` -> PASS
-   `yarn validate` -> PASS
-   Notes: final confirmation rerun after appending same-day verification evidence.
-   `yarn validate` -> PASS
-   Notes: verification run for `WP-008-29` `PR-001` closure after updating plan/epic/CURRENT/AGENTS tracking.
-   `yarn test:p0` -> PASS
-   `yarn validate` -> PASS
-   Notes: verification run for `WP-008-29` `PR-002` closure after mapper error-mapping changes and tracking sync.
-   `yarn validate` -> PASS
-   Notes: final rerun after updating all required tracking artifacts for `PR-002` (`AGENTS.md`, `CURRENT.md`, active epic, and parity execution doc).
-   `yarn vitest run -c vitest.p0.config.ts packages/rankup/test/__tests__/p0/contracts/tournaments.contract.test.ts packages/rankup/test/__tests__/p0/contracts/submissions.contract.test.ts` -> PASS
-   `yarn test:p0` -> PASS
-   `yarn validate` -> PASS
-   Notes: verification run for `WP-008-29` `PR-003` closure after passthrough updates and tracking sync.
-   `yarn validate` -> PASS
-   Notes: final rerun after recording `PR-003` tracking updates in all mandatory artifacts.
-   `yarn vitest run -c vitest.p0.config.ts packages/rankup/test/__tests__/p0/smoke/flows.smoke.test.ts` -> PASS
-   `yarn test:p0` -> PASS
-   `yarn validate` -> PASS
-   Notes: verification run for `WP-008-29` `PR-004` closure after `scoring/results` wiring, smoke flow #3 update, and required tracking sync (`AGENTS.md`, `CURRENT.md`, active epic, parity plan, daily log).
-   `yarn validate` -> PASS
-   Notes: final confirmation rerun after appending PR-004 verification evidence in today log.
-   `yarn test:guardrails:p0` -> PASS
-   `yarn test:guardrails:p0` -> FAIL
-   Notes: expected-fail validation using temporary `apps/rankup-spa/services/api/tournament/ratchet-check-gateway.ts` with no mapper suite; guardrail correctly reported required `__tests__/p0/mappers/*.mapper.test.ts` naming options.
-   `yarn validate` -> PASS
-   Notes: verification run for `WP-008-29` `PR-005` closure after dynamic gateway ratchet rollout and mandatory tracking sync.
-   `yarn test:guardrails:p0` -> PASS
-   `yarn validate` -> PASS
-   Notes: final confirmation rerun after updating `PR-005` tracking artifacts (`AGENTS.md`, `CURRENT.md`, active epic, parity plan, daily log).
-   `yarn validate` -> PASS
-   Notes: final post-log-update rerun to keep verification evidence aligned with the latest `PR-005` tracking edits.
-   `yarn validate` -> PASS
-   Notes: baseline entry verification before starting `WP-008-29` `PR-006`.
-   `yarn test:determinism:p0` -> PASS
-   `yarn test:budget:p0` -> PASS
-   `yarn test:p0:ci` -> PASS
-   `yarn test:determinism:p0` -> FAIL
-   Notes: expected-fail probe with temporary `packages/testkit/src/__tests__/p0/sanity/determinism-fail.test.ts` containing `Date.now()`.
-   `P0_BUDGET_TOTAL_MS=1 yarn test:budget:p0` -> FAIL
-   Notes: expected-fail probe confirming total-runtime budget enforcement path.
-   `yarn test:determinism:p0` -> PASS
-   `yarn test:budget:p0` -> PASS
-   `yarn validate` -> PASS
-   Notes: final confirmation run after syncing PR-006 tracking updates (`AGENTS.md`, `CURRENT.md`, active epic, parity plan, daily log).
-   `yarn validate` -> PASS
-   Notes: final post-log-update rerun to keep PR-006 verification evidence aligned with the latest same-day log edits.
-   `yarn validate` -> PASS
-   Notes: baseline entry verification before starting `WP-008-29` `PR-007`.
-   `yarn vitest run -c vitest.p0.config.ts packages/rankup/test/__tests__/p0/contracts/scoring.contract.test.ts` -> PASS
-   Notes: added explicit scoring-results success contract coverage (`getMyMatchdayResults`) to satisfy final structural coverage closure.
-   `yarn test:guardrails:p0` -> PASS
-   `yarn test:p0` -> PASS
-   `yarn engine:test` -> PASS
-   `yarn validate` -> PASS
-   Notes: PR-007 closure evidence lock run after final doc sync across `AGENTS.md`, `CURRENT.md`, active epic, and parity execution plan.
-   `yarn validate` -> PASS
-   Notes: baseline entry verification before executing `WP-008-30` naming migration.
-   `rg -n "services/api/tournament/|\\.\\./tournament/" apps/rankup-spa packages/rankup scripts -g "*.ts"` -> PASS
-   Notes: only expected guardrail message literals remain in `scripts/repo-guardrails.ts`; no code imports use legacy `tournament` API path.
-   `yarn test:guardrails:p0` -> PASS
-   `yarn test:p0` -> PASS
-   `yarn validate` -> PASS
-   Notes: final verification after `WP-008-30` code + guardrail + docs tracking updates.
-   `yarn test:guardrails:p0` -> PASS
-   `yarn test:p0` -> PASS
-   `yarn engine:test` -> PASS
-   `yarn validate` -> PASS
-   Notes: final verification after `WP-008-31` canonical naming closure and tracking sync.
-   `yarn validate` -> PASS
-   Notes: final post-tracking rerun after updating `WP-008-31` tracking text and skill path references.
-   `yarn validate` -> PASS
-   Notes: final confirmation rerun after appending `WP-008-31` verification evidence.
-   `yarn validate` -> PASS
-   Notes: verification run after activating `WP-008-32` planning artifacts (`AGENTS.md`, `CURRENT.md`, active epic, and `docs/work/epics/008-operation-coverage-4way-global.md`).
-   `yarn validate` -> PASS
-   Notes: final post-log-update rerun after recording `WP-008-32` planning evidence in the daily log.
-   `yarn validate` -> PASS
-   Notes: final confirmation rerun after all `WP-008-32` planning docs were settled.
-   `yarn repo:operation-coverage:report` -> PASS
-   Notes: generated reproducible global inventory at `diagnostics/operation-coverage-global-report.json` (`226` non-admin operations, `missingOwner=32`, `fallbackMockHandlerForbidden=190`, `missingGatewayMapping=226`, `nonCanonicalFixturePath=32`).
-   `yarn validate` -> PASS
-   Notes: verification run after completing `WP-008-32` `PR-001` code/docs/tracking updates.
-   `yarn repo:operation-coverage` -> PASS
-   Notes: global gate active over all `226` non-admin operations (`defaultMockHandlers=226/226`, `fixtures=226/226`, `missingOwner=32` tracked as non-blocking debt for `PR-004`).
-   `yarn gateways:ownership` -> PASS
-   Notes: ownership diagnostics now globalized and aligned to the same `missingOwner=32` non-blocking debt posture.
-   `yarn api-mock:schema-validate` -> PASS
-   `yarn api-http:schema-validate` -> PASS
-   `yarn validate` -> PASS
-   Notes: verification run after completing `WP-008-32` `PR-002` (single global `repo:operation-coverage` gate wired into validate + tracking sync).
-   `yarn validate` -> PASS
-   Notes: final confirmation rerun after ADR/epic/CURRENT/AGENTS/log synchronization for `WP-008-32` `PR-002`.
-   `yarn validate` -> PASS
-   Notes: final confirmation rerun after clarifying transitional `missingOwner` enforcement timing (`PR-004`) in the `WP-008-32` execution document.
-   `yarn waivers:check` -> PASS
-   `yarn waivers:report` -> PASS
-   `WAIVERS_MAX_TOTAL=10 node --import tsx scripts/repo-operation-waivers.ts check` -> FAIL
-   Notes: expected-fail probe with temporary legacy waiver (`expiresOn`, missing `plan`) correctly failed with schema-v2 errors, then `diagnostics/operation-waivers.json` was restored to `[]`.
-   `yarn validate` -> PASS
-   Notes: final confirmation rerun after completing all `WP-008-32` `PR-003` code/docs/tracking updates.
-   `yarn validate` -> PASS
-   Notes: post-log-update final confirmation for `WP-008-32` `PR-003` closure evidence.
-   `yarn validate` -> PASS
-   Notes: final post-log-update verification for `WP-008-32` `PR-003`.
-   `yarn gateways:ownership` -> PASS
-   Notes: strict ownership gate now enforces central `gatewayOperationMapping` parity and reports `owners=226/226`, `mapping=226/226`.
-   `yarn repo:operation-coverage` -> PASS
-   Notes: PR-004 coverage gate now blocks on `missingOwner` + `missingGatewayMapping` + owner/mapping mismatch and passes at full non-admin scope.
-   `yarn repo:operation-coverage:report` -> PASS
-   Notes: refreshed `diagnostics/operation-coverage-global-report.json` (`owners=226/226`, `gatewayMapping=226/226`, residual global failures now concentrated in fallback handlers + non-canonical fixture paths).
-   `yarn validate` -> PASS
-   Notes: final verification run after PR-004 code changes and mandatory tracking updates.
-   `yarn vitest run -c vitest.p0.config.ts apps/rankup-spa/services/api/problem/__tests__/p0/mapProblemToDomainError.mapper.test.ts apps/rankup-spa/services/api/problem/__tests__/p0/httpClient.problem-mapping.test.ts packages/rankup/test/__tests__/p0/smoke/problemDetails.smoke.test.ts` -> PASS
-   Notes: targeted verification for `WP-008-33` `PR-001` runtime canonical mapping + taxonomy additions.
-   `yarn test:p0` -> PASS
-   Notes: full P0 lane remains green after adding canonical error mapper/smoke suites.
-   `yarn validate` -> PASS
-   Notes: final verification run after `WP-008-33` `PR-001` code + ADR + tracking updates.
-   `yarn test:p0` -> PASS
-   Notes: post-formatting rerun after final `http-client` indentation cleanup in PR-001.
-   `yarn validate` -> PASS
-   Notes: final post-cleanup verification rerun for `WP-008-33` `PR-001`.
