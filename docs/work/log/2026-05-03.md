# Work log - 2026-05-03

## Summary

-   Followed mandatory entry procedure (`START-HERE`, `CURRENT`, active epic, latest daily log).
-   Confirmed the tree was clean and commit `3affae1` deleted `packages/api/openapi.yaml`.
-   Baseline `yarn validate` failed because the canonical OpenAPI spec was missing.
-   Restored `packages/api/openapi.yaml` from `HEAD^` and reapplied the contract changes implied by generated artifacts in `HEAD`, including `MatchStatus.x-rankup-canonical-statuses`, idempotency/ETag descriptions, immutable `rulesetId` update semantics, submission timestamp descriptions, and the `CreateDuelRequest` `allOf` validation fix.
-   Confirmed `yarn openapi:verify` passes with no generated artifact drift.
-   Added a minimal generated OpenAPI doc delta plus an api-mock source comment so the existing OpenAPI ratchet sees the restored SOT, generated artifact, and mock boundary updated in the same working set.
-   Updated structural tracking artifacts (`AGENTS.md`, `docs/work/CURRENT.md`, active epic, and this log) without changing active `WP-008-35` PR status.

## Verification

-   `yarn validate` -> FAIL
-   Notes: baseline failed before restoration because `repo:guardrails` reported `Canonical OpenAPI spec missing: packages/api/openapi.yaml`.
-   `yarn openapi:verify` -> PASS
-   `yarn validate` -> FAIL
-   Notes: first post-restore run reached `api-mock:schema-validate` and failed because restored `CreateDuelRequest` still had the stale `allOf` `additionalProperties: false` extension-object conflict.
-   `yarn api-mock:schema-validate` -> PASS
-   `yarn validate` -> PASS
