# Work log - 2026-02-06

## Summary

-   Started Epic 009 for landing delivery + localization ID policy rollout.
-   Added ADR 0057 for `apps/rankup-web` landing runtime/animation stack.
-   Added ADR 0058 for global Lit Localize explicit-id policy.
-   Implemented the new landing runtime in `apps/rankup-web`:
    -   cinematic preloader (first visit only), Hero, sticky mobile CTA,
    -   sections 2..11,
    -   interactive ranking/live-goal simulator,
    -   persistent particle canvas,
    -   locale switching (`es`/`en`),
    -   SEO metadata + OpenGraph + Twitter cards + JSON-LD.
-   Added `scripts/localize-add-msg-ids.ts` and ran big-bang backfill for missing `msg()` IDs.
-   Added global `msg()` ID guardrail in `scripts/repo-guardrails.ts` with regex enforcement.
-   Updated work tracking (`CURRENT`, `ROADMAP`, Epic 009) and ADR index.
-   Implemented Landing V2 game-first refactor in `apps/rankup-web`:
    -   visual direction upgraded to competitive HUD aesthetic,
    -   hero/live copy rewritten in ES/EN with stronger Rankup positioning,
    -   GSAP choreography upgraded (master entrance + scroll depth + Flip-based ranking reorders),
    -   per-section interaction upgrades (mode tilt, social spring bubbles, event-card parallax),
    -   particle field upgraded to layered trails + attractor/burst tuning + mobile frame budget profile,
    -   `index.html` metadata/fonts updated for the new messaging.
-   Profiled landing performance and captured Lighthouse reports for WP-009-04 evidence.
-   Product decision: landing performance hardening (WP-009-04 / Epic 009) moved to backlog until explicit resume.
-   Completed WP-008-28 (ADR-0056 burn-down slice for tournaments/submissions):
	- added 30 fixtures under `packages/api-mock/src/fixtures/tournaments/`,
	- added explicit `operationOwners` in core/scoring/engagement/submissions gateways,
	- removed targeted waivers (`missingFixture`, `missingOwner`, and selected `httpFidelityMissing`),
	- lowered waiver budget to `WAIVERS_MAX_TOTAL=700`,
	- updated ADR 0056 rollout note and work tracking docs.
-   Added scoped `schemaValidationFlaky` waivers for operations still returning non contract-valid mock payloads during this iteration to keep gates actionable while burn-down continues.
-   Completed the schema-flaky follow-up for the WP-008-28 tournaments/submissions slice:
	- aligned OpenAPI schemas used by the affected operations (`TournamentSummary`, `TournamentMatchdaySummary`, `Match`, `MatchdaySubmission*`, `MatchdayResults*`) so mock payloads validate contract-first,
	- removed the targeted 12 `schemaValidationFlaky` waivers from `diagnostics/operation-waivers.json`,
	- kept `api-mock` and `api-http` schema validation gates green for the slice without adding new waiver types.
-   Continued ADR-0056 burn-down on the same tournaments scope by adding explicit core handlers + OpenAPI-contract context mapping for `tournaments.core` and `tournaments.lifecycle` operations:
	- implemented handlers for `getTournament`, `getTournamentRules`, `listDiscoverableTournaments`, `updateTournament`, `archiveTournament`, `deleteTournament`, `lockTournament`, `transferTournamentOwnership`, `unarchiveTournament`, `unlockTournament`,
	- removed their 10 `missingMockHandler` waivers from `diagnostics/operation-waivers.json`,
	- current waiver totals after that step: `652` (`httpFidelityMissing=216`, `missingFixture=196`, `missingMockHandler=205`, `missingOwner=35`).
-   Continued ADR-0056 burn-down for tournaments `submissions|rankings|results|live`:
	- added explicit core handlers + OpenAPI-contract context mapping for 13 operations (`clearMyMatchdaySubmission`, `getMyMatchdaySubmission`, `upsertMyMatchdaySubmission`, `getUserMatchdaySubmission`, `listMatchdaySubmissions`, `listTournamentMatchdayRanking`, `getMyTournamentMatchdayRankingWindow`, `getMyTournamentSeasonRankingWindow`, `getMyMatchdayResults`, `getUserMatchdayResults`, `getMatchdayResultsSummary`, `listTournamentUpdates`, `streamTournamentLive`),
	- extended `apps/rankup-spa/services/api/http-client.ts` with operationId-named methods for rankings/results/live (including SSE `streamTournamentLive`) and enabled SSE validation in `scripts/api-http-schema-validate.ts`,
	- removed 13 `missingMockHandler` waivers and 9 `httpFidelityMissing` waivers from `diagnostics/operation-waivers.json`,
	- current waiver totals: `630` (`httpFidelityMissing=207`, `missingFixture=196`, `missingMockHandler=192`, `missingOwner=35`).
-   Continued ADR-0056 cross-slice burn-down outside tournaments:
	- added 46 fixtures under `packages/api-mock/src/fixtures/generated/` for the residual non-admin operations,
	- removed all residual `missingMockHandler`, `missingFixture`, and `httpFidelityMissing` waivers,
	- kept residual `schemaValidationFlaky` waivers for operations where sampler responses are still not contract-valid,
	- current waiver totals: `81` (`missingOwner=35`, `schemaValidationFlaky=46`).
-   Executed Epic 010 baseline implementation pass:
	- completed ADR pack 0059..0067 + ADR index updates,
	- updated OpenAPI for immutable `rulesetId` update semantics, explicit submissions timestamps semantics, and idempotency/ETag conflict language; regenerated `@rankup/api` artifacts,
	- implemented engine `registry/algorithms/runtime` baseline (`packages/rankup/src/{registry,algorithms,runtime,shared}`) with deterministic scoring/tie-breakers, idempotency scope+fingerprint checks, ETag parsing, event dedupe, recompute lineage, and lifecycle evaluation,
	- added `yarn engine:test` and deterministic runtime tests under `packages/rankup/test/engine-runtime.test.ts`,
	- introduced formal parity baseline operations list (`diagnostics/parity-baseline-operations.json`) and updated coverage/ownership/schema/http scripts to enforce baseline by `operationId`,
	- emptied `diagnostics/operation-waivers.json` and ratcheted `WAIVERS_MAX_TOTAL=0`,
	- added parity baseline quality doc and engine add-sport/mode checklist.
-   Closed Epic 010 residual runtime/mock integration:
	- added `CancelTournamentUseCase` (staff override) and enforced `cancelled` effects on joins/submissions in runtime policies/tests,
	- integrated api-mock tournaments core/results/submissions/rankings/live handlers through a runtime-backed harness with deterministic in-memory state,
	- enabled async mock handlers end-to-end in api-mock registry/openapi-contract + generated mock client callers,
	- aligned scenario/schema/http validation paths to reset runtime+db state on each request (`x-rankup-mock-reset`) to avoid flaky cross-request state.
-   Normalized mock DB season defaults to avoid static `2024` drift:
	- `createMockDb` now derives season dynamically from clock year by default and supports explicit configuration via `MockDbOptions` (`now`, `seasonId`),
	- competitions and matches seed data now use the resolved season instead of hardcoded values,
	- registry/client wiring now accepts `dbOptions` to keep season/clock reproducible in runtime tests and mock scenarios.
-   Continued Epic 010 with post-baseline parity expansion slice (`WP-010-F1`):
	- expanded `diagnostics/parity-baseline-operations.json` to include `listCompetitions`, `listMyDuels`, `createDuel`, and `createDuelRematch` (baseline now `34` operations),
	- fixed `createDuel` fixture/body validation and handler defaults for contract-valid request/response behavior,
	- corrected OpenAPI `CreateDuelRequest` allOf inconsistency by removing conflicting `additionalProperties: false` in the extension object and regenerated OpenAPI artifacts.
-   Continued Epic 010 with post-baseline parity expansion slice (`WP-010-F2`):
	- expanded `diagnostics/parity-baseline-operations.json` to include `getUserPublicProfile`, `oauthAuthorize`, and `oauthTokenExchange` (baseline now `37` operations),
	- added explicit `operationOwners` mapping in accounts users/auth gateways for the new baseline operations,
	- added HTTP operationId aliases for `getUserPublicProfile` and OAuth methods in SPA API client,
	- fixed OAuth authorize query serialization to omit absent optional PKCE params and prevent `400` drift in `api-http:schema-validate`.
-   Ran strict acceptance audit against CA-A..L and re-opened Epic 010 under `WP-010-F3`:
	- documented full pending `FAIL`/`PARTIAL` criteria as ordered checklist (`AC-010-F3-01` .. `AC-010-F3-18`),
	- moved execution priority from parity expansion to acceptance closure,
	- left the test-system migration lane explicitly at the end of the checklist (target: Vitest, pending confirmation).
-   Executed `WP-010-F3` `AC-010-F3-01`:
	- runtime now persists deterministic 4xx outcomes (Problem Details body + status) for idempotent flows in `joinTournament` and `upsertMatchdaySubmission`,
	- same-key retries now replay stored canonical 4xx outcomes even if domain state changes after the first request,
	- added engine tests for deterministic 4xx replay on join-locked and upsert-not-member scenarios.
-   Executed `WP-010-F3` `AC-010-F3-02`:
	- `ClearMatchdaySubmissionUseCase` now applies strong idempotency semantics (`scope+fingerprint`, replay, conflict detection, deterministic 4xx persistence),
	- `X-Idempotency-Key` is now propagated end-to-end for clear submissions (`apps/rankup-spa` HTTP client -> openapi mock contract context -> core handler -> runtime clear use-case),
	- added deterministic engine tests for clear replay (`204` replay after state drift and `409` replay after lock-state drift).
-   Executed `WP-010-F3` `AC-010-F3-03`:
	- OpenAPI mock server now supports handler-level response headers and propagates them to HTTP responses,
	- parity gates now enforce response headers declared by OpenAPI per operation/status (`X-Request-Id`, `ETag`, `Location`, and any operation-declared headers),
	- response header defaults are now synthesized from OpenAPI header definitions/examples when handlers do not set them explicitly, and schema/http validation harnesses now run with manual redirect mode for OAuth redirect responses.
-   Executed `WP-010-F3` `AC-010-F3-18`:
	- standardized engine testing on Vitest with dedicated Node-only config (`vitest.engine.config.ts`) and deterministic setup (`packages/rankup/test/setup.ts`),
	- migrated the runtime suite from `node:test` to `vitest` APIs and extracted reusable runtime fixtures into `packages/rankup/test/testkit/runtime-fixtures.ts`,
	- switched `engine:test` to Vitest while keeping `engine:test:legacy` as a temporary diagnostic bridge,
	- added engine test discipline guardrails in `repo:guardrails` (`no node:test/node:assert` and `no .only/.skip` under `packages/rankup/test/**`),
	- created Codex skill `rankup-engine-testing-vitest` and indexed it in `.codex/skills/README.md`.
-   Implemented the unified P0 test lane (PR-001..PR-007 slice) to execute test-system-first before remaining `WP-010-F3` acceptance items:
	- added root Vitest P0 configs (`vitest.config.base.ts`, `vitest.p0.config.ts`, `vitest.setup.ts`) and docs (`docs/engineering/testing-p0.md`),
	- created workspace `@rankup/testkit` with deterministic helpers/fixtures/assertions + smoke/streaming harness utilities,
	- added mapper suites (9), domain contract suites (3), runtime smoke flows (3), and streaming reconnect/dedupe/ordering suites under `__tests__/p0`,
	- added `packages/api-mock/src/create-server.ts` + exports for programmatic mock-server boot in tests,
	- added P0 guardrail ratchet script (`scripts/test-guardrails-p0.ts`) and wired it into `repo:guardrails`,
	- added CI workflow gate for P0 lane (`.github/workflows/test-p0.yml`).
-   Stabilized the post-migration test split:
	- fixed moved `packages/rankup/test/**` contract/fake imports,
	- hardened smoke tests to resolve composition-root services via service-id lookup in the instantiated container (avoids identifier drift in mixed build states),
	- narrowed `engine:test` (`vitest.engine.config.ts`) to `packages/rankup/test/engine*.test.ts` and excluded `packages/rankup/test/__tests__/p0/**` so `yarn validate` remains deterministic while P0 stays in `yarn test:p0`.
-   Executed `WP-010-F3` `AC-010-F3-04`:
	- added explicit incremental-vs-full recompute equivalence coverage in `packages/rankup/test/engine-runtime.test.ts`,
	- compares canonicalized latest snapshots (season + matchday scopes) between incremental `ApplyMatchFinishedUseCase` processing and one-shot `RecomputeTournamentUseCase`,
	- excludes volatile snapshot metadata (`snapshotId`, `parentSnapshotId`, `computedAt`, `reason`) while asserting deterministic ranking state/entries parity.
-   Executed `WP-010-F3` `AC-010-F3-05`:
	- hardened `JoinTournamentUseCase` gating to include `finished` and to map join conflicts with explicit stable `Problem.code` values (`tournamentArchived`, `tournamentCancelled`, `joinClosed`, `tournamentFull`, `tournamentLocked`),
	- updated engine runtime error taxonomy/types to include the new join conflict codes with dedicated Problem type URIs,
	- added deterministic engine coverage for finished-state joins and explicit join conflict mapping cases (archived/cancelled/closed-window/full), and aligned the cancellation join assertion to `tournamentCancelled`.
-   Executed `WP-010-F3` `AC-010-F3-06`:
	- expanded `diagnostics/parity-baseline-operations.json` with `joinTournament` and `joinTournamentByInvitationCode`,
	- added runtime-backed join handlers in `@rankup/api-mock` plus OpenAPI contract context mapping (including `X-Idempotency-Key` propagation),
	- moved join fixtures into `packages/api-mock/src/fixtures/tournaments/` and added HTTP client idempotency-header parity support for both join operations.
-   Executed `WP-010-F3` `AC-010-F3-07`:
	- added explicit engine scoring coverage for `finalOutcomeType=penalty_shootout` under `scorePrediction` default ruleset (`resultScope=extra_time`, penalties excluded),
	- added runtime ranking coverage proving penalty-shootout matches are scored from the scoreline scope and not shootout goals.
-   Executed `WP-010-F3` `AC-010-F3-08`:
	- added deterministic runtime lock coverage for kickoff boundary behavior (`now >= scheduledAt` locks submissions even with `lockState=open`),
	- added before/at-kickoff transition coverage using controlled runtime clock to prove reproducible upsert outcomes over time,
	- added pure algorithm boundary coverage for `kickoffWithGrace` (`graceSeconds`) to enforce deterministic time-window locking semantics.
-   Executed `WP-010-F3` `AC-010-F3-09`:
	- added an adversarial concurrency test for `JoinTournamentUseCase` that forces two simultaneous joins by the same user to overlap at `membershipRepo.add`,
	- verified robust race outcome: exactly one `201 joined=true`, one `200 joined=false`, a single persisted membership row for `tournamentId+userId`, and a single `tournament.member_joined` event.
-   Executed `WP-010-F3` `AC-010-F3-10`:
	- added provider/canonical drift coverage in `ApplyMatchFinishedUseCase` runtime tests,
	- verified unknown canonical `matchId` is rejected (`404 notFound`) without snapshot mutations,
	- verified identical result payloads on different canonical `matchId`s are processed independently (no cross-match fingerprint dedupe).
-   Executed `WP-010-F3` `AC-010-F3-11`:
	- published per-operation baseline Problem Details catalog as machine-readable diagnostics (`diagnostics/parity-baseline-problem-codes.json`),
	- published quality-facing catalog docs (`docs/quality/operation-problem-catalog-baseline.md`) with explicit operation/profile mapping and stable `status + Problem.code` envelopes,
	- linked the new catalog from parity-baseline maintenance rules and API error docs (`docs/quality/operation-parity-baseline.md`, `packages/api/docs/errors/README.md`).
-   Executed `WP-010-F3` `AC-010-F3-12`:
	- extended runtime domain-event envelope metadata with `requestId` and enforced consistent `requestId/correlationId/causationId` propagation in all event-emitting use-cases (`join`, `upsert`, `clear`, `cancel`, `recompute`),
	- added deterministic metadata fallback policy (`correlationId`/`causationId` defaulting from request context, and sports-event recompute causation defaulting from source `eventId`),
	- added explicit engine coverage validating metadata propagation for join, upsert, and apply-match-finished/recompute event flows.
-   Executed `WP-010-F3` `AC-010-F3-13`:
	- added enforceable no-PII logging guardrail `scripts/repo-engine-no-pii-logging.ts` to block direct log sinks (`console.*`, `logger.*`, `process.stdout/stderr.write`) in engine/runtime source scope,
	- integrated the guardrail into `repo:guardrails` so it runs as part of `yarn validate`,
	- documented policy scope and exception process in `docs/quality/engine-no-pii-logging-policy.md`, and linked enforcement in ADR 0065 implementation plan.
-   Executed `WP-010-F3` `AC-010-F3-14`:
	- added `.github/CODEOWNERS` with explicit ownership for engine core paths (`domains|algorithms|registry|runtime|shared`), OpenAPI/api-mock surfaces, parity manifests, and critical gate/guardrail scripts,
	- aligned tracking docs (`AGENTS.md`, `docs/work/CURRENT.md`, Epic 010 checklist) to mark `AC-010-F3-14` complete and move focus to `AC-010-F3-15/16/17`.
-   Executed `WP-010-F3` `AC-010-F3-15`:
	- added `scripts/repo-engine-portability-boundary.ts` as a dedicated guardrail for engine-neutral runtime boundaries in `packages/rankup/src/{runtime,algorithms,registry,shared}`,
	- enforced blocking checks for non-portable imports (`@rankup/api`, `@rankup/api-mock`, `@rankup/platform`, UI/browser paths, Node built-ins/`node:*`) and runtime global usage (`fetch`, browser globals, `process`/`Buffer`, `__dirname`/`__filename`),
	- integrated the guardrail into `scripts/repo-guardrails.ts` and updated structural tracking docs (`AGENTS.md`, `docs/work/CURRENT.md`, Epic 010 checklist).
-   Executed `WP-010-F3` `AC-010-F3-16`:
	- added `packages/rankup/test/engine-replay.test.ts` with deterministic replay assertions that execute the same scenario twice and require byte-identical serialized outputs,
	- added golden fixture `packages/rankup/test/golden/replay-scoreprediction-v1.json` and enforced exact byte-level equality against runtime replay payload output,
	- updated structural tracking docs (`AGENTS.md`, `docs/work/CURRENT.md`, Epic 010 checklist) to mark replay/golden determinism closure and move focus to `AC-010-F3-17`.
-   Executed `WP-010-F3` `AC-010-F3-17`:
	- hardened runtime duplicate-event dedupe from non-atomic `has+mark` checks to atomic `markIfAbsent` claims in processed-event repositories/use-cases, eliminating duplicate snapshot races under concurrent apply/recompute execution,
	- hardened submission concurrency for `If-Match` updates by adding compare-and-set persistence (`SubmissionRepo.saveIfVersion`) and enforcing deterministic `412 etagMismatch` losers under concurrent same-ETag races,
	- added dedicated concurrency stress coverage (`packages/rankup/test/engine-concurrency.test.ts`) for join contention, upsert `If-Match` races, and duplicate apply-event races.
-   Executed Epic 010 final acceptance closure:
	- ran final strict acceptance verification set (`yarn test:p0`, `yarn engine:test`, `yarn validate`) with all checks green,
	- marked Epic 010 status as completed in `docs/work/epics/010-rankup-engine-vscode-baseline-v1.md` and recorded final acceptance evidence section,
	- updated structural tracking (`AGENTS.md`, `docs/work/CURRENT.md`, `docs/work/ROADMAP.md`) to move active focus back to Epic 008 support lane.

## Verification

-   `yarn validate` -> FAIL
-   Notes: `repo:ratchet` failed due generated JS artifacts in `apps/rankup-web/src/*.js` after running `yarn workspace @rankup/web build`; resolved with `yarn workspace @rankup/web clean`.
-   `yarn validate` -> PASS
-   `yarn validate` -> FAIL
-   Notes: `repo:work-log` failed because structural files changed without a fresh entry in `docs/work/log/2026-02-06.md`; resolved by appending this section before rerunning full validation.
-   `yarn typecheck:workspace` -> PASS
-   `yarn openapi:generate` -> PASS
-   `yarn engine:test` -> PASS
-   `yarn waivers:check` -> PASS
-   `yarn api-mock:coverage` -> PASS
-   `yarn gateways:ownership` -> PASS
-   `yarn api-mock:schema-validate` -> PASS
-   `yarn api-http:schema-validate` -> PASS
-   `yarn clean` -> PASS
-   `yarn workspace @rankup/web build` -> PASS
-   `npx -y lighthouse http://localhost:8001/ --only-categories=performance,accessibility,best-practices,seo --output=json --output-path=diagnostics/lighthouse-rankup-web-mobile.json` -> PASS
-   `npx -y lighthouse http://localhost:8001/ --only-categories=performance,accessibility,best-practices,seo --preset=desktop --output=json --output-path=diagnostics/lighthouse-rankup-web-desktop.json` -> PASS
-   Notes: Lighthouse evidence captured but performance target remains open (`mobile performance=58`, `desktop performance=77`; mobile `LCP=10.5s`, `CLS=0.025`, `TBT=120ms`).
-   `yarn validate` -> FAIL
-   Notes: `repo:ratchet` failed after `@rankup/web` build generated JS artifacts under `apps/rankup-web/src/*.js`; cleaned with `yarn workspace @rankup/web clean`.
-   `yarn validate` -> PASS
-   `yarn validate` -> PASS
-   Notes: verification run after moving Epic 009/WP-009-04 to backlog and updating work tracking (`CURRENT`, `ROADMAP`, Epic 009).
-   `yarn validate` -> PASS
-   `yarn gateways:ownership` -> PASS
-   `yarn api-mock:schema-validate` -> FAIL
-   Notes: New fixtures exposed pre-existing mock/schema mismatches plus optional-body validation edge cases (`lockTournament`, `deleteTournament`, `upsertMyMatchdaySubmission`); fixed fixture payloads and added scoped `schemaValidationFlaky` waivers for remaining non contract-valid mock responses.
-   `yarn api-mock:schema-validate` -> PASS
-   `yarn api-http:schema-validate` -> PASS
-   `yarn waivers:check` -> PASS
-   `yarn validate` -> FAIL
-   Notes: `repo:ratchet` failed due generated `.js` artifacts in source trees after local validation tooling runs; resolved by running `yarn clean`.
-   `yarn clean` -> PASS
-   `yarn validate` -> PASS
-   `yarn openapi:generate` -> PASS
-   `yarn api-mock:schema-validate` -> PASS
-   `yarn api-http:schema-validate` -> PASS
-   `yarn waivers:check` -> PASS
-   `yarn validate` -> PASS
-   `yarn api-mock:coverage` -> PASS
-   `yarn api-mock:schema-validate` -> PASS
-   `yarn gateways:ownership` -> PASS
-   `yarn api-http:schema-validate` -> PASS
-   `yarn waivers:check` -> PASS
-   `yarn validate` -> PASS
-   `yarn api-mock:coverage && yarn api-mock:schema-validate && yarn api-http:schema-validate` -> PASS
-   `yarn validate` -> PASS
-   `yarn validate` -> PASS
-   `yarn validate` -> PASS
-   `yarn api-mock:coverage && yarn gateways:ownership && yarn api-mock:schema-validate && yarn api-http:schema-validate` -> PASS
-   `yarn validate` -> PASS
-   `yarn openapi:generate` -> PASS
-   `yarn api-mock:schema-validate && yarn api-http:schema-validate && yarn api-mock:coverage && yarn gateways:ownership` -> PASS
-   `yarn validate` -> PASS
-   `yarn api-mock:coverage` -> PASS
-   `yarn waivers:check` -> PASS
-   `yarn api-mock:schema-validate` -> PASS
-   `yarn gateways:ownership` -> PASS
-   `yarn api-http:schema-validate` -> PASS
-   `yarn typecheck:workspace` -> PASS
-   `yarn validate` -> FAIL
-   Notes: `repo:ratchet` failed due generated `.js` artifacts from workspace builds/typecheck; cleaned by deleting generated `.js` under `apps/rankup-spa` and `packages/{api,api-mock,base,common,platform,rankup,samba}`.
-   `yarn validate` -> PASS
-   `yarn validate` -> FAIL
-   Notes: `repo:ratchet` failed due generated `.js` artifacts in `apps/rankup-spa` and workspace packages from local tool runs; resolved with `yarn clean`.
-   `yarn clean` -> PASS
-   `yarn validate` -> PASS
-   `yarn validate` -> PASS
-   `yarn validate` -> PASS
-   `yarn validate` -> PASS
-   Notes: baseline re-check before updating tracking docs for `WP-010-F3` acceptance-closure lane.
-   `yarn validate` -> PASS
-   Notes: final verification after updating `CURRENT`, Epic 010 (`WP-010-F3`), and ADR pending decision tracking.
-   `yarn validate` -> PASS
-   Notes: final re-run after appending verification evidence to this log entry.
-   `yarn validate` -> PASS
-   Notes: final confirmation run after the last verification-note append.
-   `yarn engine:test` -> PASS
-   `yarn validate` -> PASS
-   Notes: final verification after completing `AC-010-F3-12` and updating structural tracking (`AGENTS.md`, `CURRENT`, Epic 010 checklist, and this log entry).
-   `yarn engine:test` -> PASS
-   `yarn validate` -> PASS
-   Notes: final verification after completing `AC-010-F3-13` and updating structural tracking (`AGENTS.md`, `CURRENT`, Epic 010 checklist, ADR 0065, and this log entry).
-   `yarn validate` -> PASS
-   Notes: final confirmation rerun after hardening the no-PII guardrail pattern to also catch bracket-call sinks (`console['log'](...)` / `logger['info'](...)`).
-   `yarn engine:test` -> PASS
-   `yarn validate` -> PASS
-   `yarn validate` -> PASS
-   Notes: final confirmation rerun after appending `AC-010-F3-04` tracking evidence to this log.
-   `yarn validate` -> PASS
-   Notes: final rerun after appending the previous verification note.
-   `yarn validate` -> PASS
-   Notes: final run after closing `AC-010-F3-01` tracking updates (`AGENTS`, `CURRENT`, Epic 010 checklist, and this log entry).
-   `yarn validate` -> PASS
-   Notes: final confirmation rerun after appending the previous verification note.
-   `yarn validate` -> PASS
-   `yarn engine:test` -> PASS
-   `yarn validate` -> PASS
-   `yarn api-mock:schema-validate` -> FAIL
-   Notes: OAuth authorize started returning `Location` header (as required), and the validation harness followed redirects to `auth.mock`; fixed by running schema/http validators with `redirect: manual`.
-   `yarn api-mock:schema-validate` -> PASS
-   `yarn api-http:schema-validate` -> FAIL
-   Notes: local script regression from duplicated `nextHeaders` variable in `api-http-schema-validate.ts`; fixed by renaming the response header accumulator.
-   `yarn api-http:schema-validate` -> PASS
-   `yarn validate` -> PASS
-   `yarn validate` -> PASS
-   Notes: final confirmation run after `AC-010-F3-03` code + tracking updates.
-   `yarn validate` -> PASS
-   Notes: final rerun after appending the previous verification note.
-   `yarn engine:test` -> PASS
-   `yarn validate` -> PASS
-   `yarn validate` -> PASS
-   Notes: baseline re-check before executing `AC-010-F3-18` (Vitest migration lane).
-   `yarn install` -> PASS
-   Notes: lockfile updated after adding `vitest` devDependency for the engine test runner migration.
-   `yarn engine:test` -> PASS
-   `yarn validate` -> PASS
-   Notes: final verification after completing `AC-010-F3-18` tooling + suite migration + tracking updates.
-   `yarn validate` -> PASS
-   Notes: final confirmation rerun after appending `AC-010-F3-18` verification evidence.
-   `yarn validate` -> PASS
-   Notes: final confirmation rerun after adjusting `engine:test:legacy` transition script messaging.
-   `yarn install` -> PASS
-   `yarn test:p0` -> PASS
-   `yarn test:guardrails:p0` -> PASS
-   `yarn validate` -> FAIL
-   Notes: `engine:test` was unintentionally running moved `packages/rankup/test/__tests__/p0/**` suites after `typecheck:workspace` generated JS artifacts, causing mixed-module DI metadata drift and smoke failures (`gateway` undefined) plus stale contract-test import paths after file moves.
-   `yarn clean` -> PASS
-   `yarn test:p0` -> PASS
-   `yarn engine:test` -> PASS
-   `yarn test:guardrails:p0` -> PASS
-   `yarn validate` -> PASS
-   Notes: fixed moved P0 contract/fake import paths, stabilized smoke resolution using composition-root service-id lookup, and isolated `engine:test` to `packages/rankup/test/engine*.test.ts` (excluding `__tests__/p0`).
-   `yarn validate` -> PASS
-   Notes: final confirmation rerun after updating structural tracking docs for the P0/engine-test split stabilization (`CURRENT`, Epic 010, `AGENTS.md`, and this log entry).
-   `yarn validate` -> PASS
-   Notes: final rerun after appending the previous verification note.
-   `yarn engine:test` -> PASS
-   `yarn validate` -> PASS
-   Notes: final verification after completing `AC-010-F3-05` join gating + error-mapping hardening and updating structural tracking (`AGENTS.md`, `CURRENT`, Epic 010 checklist, and this log entry).
-   `yarn engine:test && yarn validate` -> PASS
-   Notes: final confirmation rerun after the last tracking append for `AC-010-F3-05`.
-   `yarn validate` -> PASS
-   Notes: final rerun after appending the previous verification note.
-   `yarn api-mock:coverage && yarn gateways:ownership && yarn api-mock:schema-validate && yarn api-http:schema-validate` -> PASS
-   `yarn engine:test` -> PASS
-   `yarn validate` -> PASS
-   Notes: final verification after completing `AC-010-F3-06` (runtime-backed join parity baseline + fixtures/owners/http parity) and updating structural tracking docs (`AGENTS.md`, `CURRENT`, Epic 010 checklist, and this log entry).
-   `yarn validate` -> PASS
-   Notes: final confirmation rerun after appending the `AC-010-F3-06` structural tracking updates.
-   `yarn engine:test` -> PASS
-   `yarn validate` -> PASS
-   Notes: final verification after completing `AC-010-F3-07` (`CA-C2`) and updating structural tracking docs (`AGENTS.md`, `CURRENT`, Epic 010 checklist, and this log entry).
-   `yarn validate` -> PASS
-   Notes: final confirmation rerun after appending the `AC-010-F3-07` structural tracking updates.
-   `yarn engine:test` -> PASS
-   `yarn validate` -> PASS
-   Notes: final verification after completing `AC-010-F3-08` (`CA-D4`) and updating structural tracking docs (`AGENTS.md`, `CURRENT`, Epic 010 checklist, and this log entry).
-   `yarn validate` -> PASS
-   Notes: final confirmation rerun after appending the `AC-010-F3-08` structural tracking updates.
-   `yarn engine:test` -> PASS
-   `yarn validate` -> PASS
-   Notes: final verification after completing `AC-010-F3-09` (`CA-E1.3`) and updating structural tracking docs (`AGENTS.md`, `CURRENT`, Epic 010 checklist, and this log entry).
-   `yarn validate` -> PASS
-   Notes: final confirmation rerun after appending the `AC-010-F3-09` structural tracking updates.
-   `yarn engine:test` -> PASS
-   `yarn validate` -> PASS
-   Notes: final verification after completing `AC-010-F3-10` (`CA-F2`) and updating structural tracking docs (`AGENTS.md`, `CURRENT`, Epic 010 checklist, and this log entry).
-   `yarn engine:test` -> PASS
-   `yarn validate` -> PASS
-   Notes: final verification after completing `AC-010-F3-11` (`CA-G2`) and publishing the baseline per-operation Problem Details catalog with structural tracking updates (`AGENTS.md`, `CURRENT`, Epic 010 checklist, and this log entry).
-   `yarn validate` -> PASS
-   Notes: final confirmation rerun after appending the `AC-010-F3-11` structural tracking updates.
-   `yarn validate` -> PASS
-   Notes: final verification after completing `AC-010-F3-14` (`CA-K1`) by adding `.github/CODEOWNERS` and updating structural tracking docs (`AGENTS.md`, `CURRENT`, Epic 010 checklist, and this log entry).
-   `yarn exec tsc -p scripts/tsconfig.json --pretty false --noEmit` -> PASS
-   `yarn repo:guardrails` -> PASS
-   `yarn validate` -> PASS
-   Notes: final verification after completing `AC-010-F3-15` (`CA-A1`) with the new engine portability/boundary guardrail integrated into `repo:guardrails` and structural tracking updates (`AGENTS.md`, `CURRENT`, Epic 010 checklist, and this log entry).
-   `yarn engine:test` -> FAIL
-   Notes: initial `AC-010-F3-16` run failed because golden fixture included trailing newline while replay serializer returned minified JSON without newline; fixed by normalizing replay serializer to emit canonical newline-terminated payload bytes.
-   `yarn engine:test` -> PASS
-   `yarn validate` -> PASS
-   Notes: final verification after completing `AC-010-F3-16` (`CA-J1`) with replay/golden byte-equivalent assertions and structural tracking updates (`AGENTS.md`, `CURRENT`, Epic 010 checklist, and this log entry).
-   `yarn engine:test` -> PASS
-   `yarn validate` -> PASS
-   Notes: final verification after completing `AC-010-F3-17` (`CA-J2`) with runtime concurrency hardening (atomic processed-event claims + `If-Match` compare-and-set persistence) and new stress-race coverage (`join/upsert/apply`).
-   `yarn test:p0` -> PASS
-   `yarn engine:test` -> PASS
-   `yarn validate` -> PASS
-   Notes: final Epic 010 acceptance closure verification after marking Epic 010 as completed and updating structural tracking (`AGENTS.md`, `CURRENT`, `ROADMAP`, Epic 010, and this log entry).
