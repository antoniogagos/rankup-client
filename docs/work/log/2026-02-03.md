# Work log - 2026-02-03

## Summary

-   Aligned Apibase build details: Rollup HTML inputs now use `apps/**/*.html`.
-   Platform env now relies on `__APP_ENV__` injection (no `env.json.example` imports).
-   Removed json.example module handling from WDS/Rollup and related typings.
-   Ignored `.tsbuildinfo` artifacts in repo outputs.
-   Fixed TS override/type-only import errors across UI/Samba/common; updated validate ordering to avoid TS6305 (clean runs after app validate); app validate now runs `tsc -b` + clean for standalone runs.
-   Removed `/tmp/rankup-clean` artifacts created during earlier cleanups.
-   Scaffolded `packages/rankup/src/domains/tournaments` with Hadron-style `models/`, `contracts/`, `analysis/`, `services/`, `fixtures/`, and `validation` plus domain README.
-   Updated Codex skills to use the new apps/rankup-spa and tourney domain paths.
-   Added LLM prompt for tourney domain scaffolding under `packages/rankup/src/domains/tournaments/LLM_PROMPT.md`.
-   WDS dev-server configs are now `.mjs` (TS-only exception) so WDS auto-loads them; start scripts use `wds --watch`.
-   Wireit is listed in app devDependencies so workspace start scripts resolve.
-   App WDS configs now call `createWebDevServerConfig(appRoot)` so SPA fallback works regardless of shell cwd.
-   Fixed WDS middleware to use `context.response.set(...)` (Koa) instead of `setHeader`.
-   WDS now maps `.ts`/`.json` to JS and uses an outside-root depth prefix so module scripts and workspace assets resolve with correct MIME types; node resolution is anchored to the repo root with an outside-root depth guard.
-   Tourney domain barrel exports are type-only to avoid duplicate identifier errors during composite builds.
-   Expanded tourney domain models/contracts/services to cover previews, matchday navigation, ranking windows, membership, invitation codes, and direct invites aligned to OpenAPI.
-   Updated tourney gateway + HTTP client mapping and api-mock parity for the new endpoints; refreshed the tourney LLM prompt.
-   Documented the OpenAPI change protocol and added gateway mapping helpers for aligned DTOs.
-   Added guardrail to forbid `...api*` spreads and `as Domain.*` assertions in gateways.

-   Added `docs/overview.md` with human onboarding of repo folders; updated `docs/README.md` index.
-   Refactored tourney domain into capability subfolders (shared/core/matchdays/ranking/members/codes/invites) with per-capability services/gateways and split app gateways under `services/api/tourney`.
-   Updated ADRs, docs, and skills to match the new tourney capability layout and AppServices shape.
-   Regenerated repo tree snapshots (`docs/diagnostics/REPO-TREE.md`, `docs/scope/questions.md`).
-   Removed legacy empty `packages/app` folder; app lives in `apps/rankup-spa`.
-   Added Rankup Engine domain partitioning proposal (`docs/architecture/rankup-engine-domain-partitioning.md`).
-   Renamed `packages/rankup/src/domains/tourney` to `packages/rankup/src/domains/tournaments` and updated references (ADR 0052).
-   Moved ranking capability into `packages/rankup/src/domains/scoring`, updated app wiring/imports, and added ADR 0053.
-   Scaffolded Rankup Engine layers + domain placeholders (README-only) and added ADR 0054.
-   Added Epic 008 WPs for Fase 0 domain implementations (accounts/sports/rules/submissions/engagement).
-   Implemented sports domain (catalog/schedule contracts + services), app gateways, and mock client support; updated service catalog and AppServices.
-   Implemented accounts domain (auth/me/users/social contracts + services), app gateways/mappers, api-mock parity, and AppServices/composition root wiring.
-   Removed placeholder tourney capability stubs now owned by engagement/submissions/scoring/algorithms.

## Verification

-   `yarn validate` -> FAIL
-   Notes: Node v24.13.0; repo:work-log failed because `docs/work/log/2026-02-03.md` was missing.
-   `yarn validate` -> FAIL
-   Notes: Node v24.13.0; TS errors for missing override modifiers/type-only imports across packages/common + packages/samba + apps/rankup-spa, TS6305 outputs not built from referenced projects, unused symbols in session-manager and app-router, and api-mock SwaggerParser typing errors.
-   `yarn validate` -> FAIL
-   Notes: Node v25.5.0; yarn not installed (`zsh: command not found: yarn`).
-   `corepack enable` -> FAIL
-   Notes: Node v25.5.0; corepack not found (`zsh: command not found: corepack`).
-   `npm i -g corepack` -> PASS
-   Notes: installed corepack globally to enable yarn.
-   `corepack enable` -> PASS
-   `corepack prepare yarn@4.12.0 --activate` -> PASS
-   `yarn validate` -> FAIL
-   Notes: repo:ratchet failed; new JS files not allowed (packages/api/src/client.js, packages/api/src/generated/openapi.js, packages/api/src/index.js, packages/api/src/types.js, packages/base/browser/disposableController.js, packages/base/browser/event.js, packages/base/common/event.js, packages/base/common/lifecycle.js).
-   `yarn validate` -> FAIL
-   Notes: TS errors remain (override modifiers, type-only imports under verbatimModuleSyntax, TS6305 missing build outputs, api-mock typing issues, session-manager typing errors, and unused vars). Generated .js/.d.ts artifacts moved aside to `/tmp/rankup-clean` to unblock repo:ratchet.
-   `yarn install` -> PASS
-   Notes: Yarn 4.12.0; removed @rankup/domain-tourney workspace; warnings about peer deps (eslint version mismatch, missing @babel/core, YN0086).
-   `yarn validate` -> FAIL
-   Notes: TS errors remain (override modifiers, type-only imports under verbatimModuleSyntax, TS6305 missing build outputs, session-manager typing errors, api-mock typing errors, and unused vars). Apibase-style domain migration moved `packages/domain-tourney` to `/tmp/rankup-domain-tourney-removed`.
-   `yarn validate` -> PASS
-   Notes: Node v25.5.0; validate now cleans after app validate to preserve project reference outputs.
-   `yarn workspace @rankup/app validate` -> PASS
-   Notes: Node v25.5.0; app validate uses `tsc --noEmit`.
-   `yarn validate` -> PASS
-   Notes: Node v25.5.0; full validate passes with updated app validate script.
-   `yarn validate` -> PASS
-   Notes: Node v25.5.0; WDS configs now load as `.mjs` (no tsx shim); app validate remains `tsc --noEmit`.
-   `yarn validate` -> PASS
-   Notes: Node v25.5.0; wireit added to app devDependencies to fix `wireit` not found in workspace start scripts.
-   `yarn validate` -> PASS
-   Notes: Node v25.5.0; app configs now call `createWebDevServerConfig(appRoot)` to avoid cwd-dependent WDS routing.
-   `yarn validate` -> PASS
-   Notes: Node v25.5.0; WDS config factory + app root override in place; `punycode` warning only.
-   `yarn validate` -> PASS
-   Notes: Node v25.5.0; `.mjs` WDS configs auto-load and `wds --watch` scripts restored; `punycode` warning only.
-   `yarn validate` -> PASS
-   Notes: Node v25.5.0; WDS configs now `.mjs` with app-rooted config factory; `punycode` warning only.
-   `yarn validate` -> PASS
-   Notes: Node v25.5.0; WDS middleware header setter corrected; `punycode` warning only.
-   `yarn validate` -> PASS
-   Notes: Node v25.5.0; WDS middleware fix confirmed; `punycode` warning only.
-   `yarn validate` -> FAIL
-   Notes: Node v25.5.0; repo:ratchet failed after `tsc -b` outputs produced new .js artifacts; cleaned before re-run.
-   `yarn clean` -> PASS
-   `yarn validate` -> PASS
-   Notes: Node v25.5.0; validate passes after tourney domain scaffolding + skill updates.
-   `yarn validate` -> FAIL
-   Notes: Node v25.5.0; TS6305 outputs missing for @rankup/rankup domain contracts after local cleanup.
-   `yarn validate` -> FAIL
-   Notes: Node v25.5.0; repo:ratchet flagged generated .js outputs after rebuild; cleaned before retry.
-   `yarn validate` -> PASS
-   Notes: Node v25.5.0; validate passes (punycode warning only).
-   `yarn validate` -> PASS
-   Notes: Node v25.5.0; WDS repo root discovery now package-name based with `nodeResolve.jail` and `preserveSymlinks=false`; `punycode` warning only.
-   `yarn validate` -> PASS
-   Notes: Node v25.5.0; WDS node resolution jailed to repo root; `punycode` warning only.
-   `yarn validate` -> PASS
-   Notes: Node v25.5.0; WDS outside-root guard + nodeResolve.rootDir/jail changes verified; `punycode` warning only.
-   `yarn validate` -> PASS
-   `yarn validate` -> PASS
-   `yarn validate` -> PASS
-   `yarn validate` -> PASS
-   Notes: Node v25.5.0; `punycode` warning only.
-   `yarn validate` -> PASS
-   Notes: Node v25.5.0; `punycode` warning only.
-   `yarn validate` -> PASS
-   Notes: Node v25.5.0; `punycode` warning only.
-   `yarn validate` -> PASS
-   Notes: Node v25.5.0; `punycode` warning only.
-   `yarn validate` -> PASS
-   Notes: Node v25.5.0; removed legacy empty `packages/app` folder; `punycode` warning only.
-   `yarn validate` -> PASS
-   Notes: Node v25.5.0; AGENTS updated for legacy `packages/app` removal; `punycode` warning only.
-   `yarn validate` -> PASS
-   Notes: Node v25.5.0; added Rankup Engine domain partitioning proposal doc; `punycode` warning only.
-   `yarn validate` -> PASS
-   Notes: Node v25.5.0; refined wording in Rankup Engine domain partitioning proposal doc; `punycode` warning only.
-   `yarn validate` -> PASS
-   Notes: Node v25.5.0; renamed tourney domain folder to tournaments and updated references; `punycode` warning only.
-   `yarn validate` -> PASS
-   Notes: Node v25.5.0; scoring domain split for rankings; `punycode` warning only.
-   `yarn validate` -> PASS
-   Notes: Node v25.5.0; Rankup Engine scaffolding for domains/layers; `punycode` warning only.
-   `yarn validate` -> PASS
-   Notes: Node v25.5.0; added Fase 0 WPs; `punycode` warning only.
-   `yarn validate` -> PASS
-   Notes: Node v25.5.0; sports domain implementation; `punycode` warning only.
-   `yarn validate` -> FAIL
-   Notes: Node v25.5.0; TS errors in api-mock rules filtering + http-client signature after rules domain work.
-   `yarn validate` -> FAIL
-   Notes: Node v25.5.0; repo:ratchet flagged generated .js outputs after rebuild.
-   `yarn clean` -> PASS
-   `yarn validate` -> PASS
-   Notes: Node v25.5.0; rules domain implemented (game modes + rulesets) with gateways, AppServices, and api-mock parity.
-   `yarn validate` -> FAIL
-   Notes: Node v25.5.0; guardrails blocked multiline imports and TS errors in api-mock/accounts oauth mapping.
-   `yarn validate` -> FAIL
-   Notes: Node v25.5.0; repo:ratchet flagged generated .js outputs after build.
-   `yarn clean` -> PASS
-   `yarn validate` -> PASS
-   Notes: Node v25.5.0; `punycode` warning only.
-   `yarn validate` -> FAIL
-   Notes: Node v25.5.0; repo:ratchet blocked generated JS outputs before cleanup.
-   `yarn clean` -> PASS
-   `yarn validate` -> PASS
-   Notes: Node v25.5.0; submissions gateway/mappers fixes; `punycode` warning only.
-   `yarn validate` -> FAIL
-   Notes: Node v25.5.0; TS errors in engagement chat models (unused MatchId) and api client/index type exports (missing content, duplicate identifiers, missing chat request exports).
-   `yarn clean` -> PASS
-   `yarn validate` -> PASS
-   Notes: Node v25.5.0; engagement domain (chat/live/stats) wired with gateways + api-mock parity; `punycode` warning only.
-   `yarn validate` -> PASS
-   Notes: Node v25.5.0; baseline validate before tournaments placeholder cleanup (punycode warning only).
-   `yarn validate` -> PASS
-   Notes: Node v25.5.0; tournaments placeholder cleanup + doc updates (punycode warning only).
