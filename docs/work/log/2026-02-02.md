# Work log - 2026-02-02

## Summary

-   Added Apibase-to-Rankup architecture alignment analysis for reuse and gaps.
-   Expanded Apibase alignment deep-dive and refreshed workspace install state after apps split.
-   Fixed domain-tourney module resolution to match platform export imports and codified no-legacy/compat shims in agent rules.
-   Removed legacy empty `packages/platform/src/tourney` folders.
-   Deferred external game-mode extensibility in Apibase alignment; added pending decision on ruleset compatibility/migrations.
-   Documented ruleset historical immutability + no-migration policy in negocio docs and P-0007.
-   Accepted internal game-mode registry + ruleset versioning (ADR 0044) and resolved P-0007.
-   Drafted OpenAPI migration plan/questions for architect (canonical spec in packages/api/openapi.yaml).
-   Recorded OpenAPI migration decisions (v3.2 canonical, fragments as design-only, strict guardrail).
-   Executed OpenAPI migration: canonical moved to packages/api/openapi.yaml, tooling/guardrails updated, legacy specs removed.
-   Added Problem Details 413/415 responses + `Vary-AcceptLanguage` header and aligned spectral rules to validate `application/problem+json` + `text/event-stream`.
-   Aligned API client/mocks/UI to updated OpenAPI types (tourney endpoints, OAuth headers, match lock state) and fixed guardrail import formatting.
-   Moved the Problem Details error catalog to `packages/api/docs/errors` and refreshed repo tree snapshots; uploads error types confirmed.
-   Updated DI docs to reflect `listMyTournaments` naming in tourney examples.
-   Pinned OpenAPI to 3.1.2 for toolchain compatibility and documented via ADR 0045.
-   Clarified ScorePrediction result scope (final score after extra time, penalties excluded) and added match outcome + penalty shootout context to the OpenAPI spec.
-   Added CreateTournament rulesetConfig examples to guide UI/mocks for ScorePrediction resultScope overrides.
-   Fixed InstantiationService to duck-type SyncDescriptor and avoid dev-server service resolution mismatches.
-   Moved API client wiring into app services and removed product SDK imports from @rankup/platform (ADR 0046).
-   Added domain DTOs + ITourneyGateway mapping, removed @rankup/api from UI/domain, and moved mock selection to the app composition root (ADR 0048).
-   Adopted Apibase-style build pipeline (root tsc -b + rollup + clean) with shared WDS/esbuild config serving TS directly in dev (ADR 0047).
-   Named Rollup HTML inputs per app, tightened rollup tsconfig excludes to skip server/scripts/config with DOM-only types, and fixed the @rankup/api guardrail regex so it no longer matches @rankup/api-mock.
-   Updated SPA bootstrap typing for overlay-container and adjusted SPA stylesheet links to resolve package paths under the Rollup build.
-   Moved SPA bootstrap into apps/rankup-spa/main.ts, keeping rk-app/rk-unauthenticated-app view-only and shifting DI/session/mock setup into the host.
-   Host now injects appServices/sessionManager into root elements and disposes session listeners via DisposableStore.
-   Removed public app context wiring from SPA bootstrap; public pages now consume ISessionManager via provider + redirect helper.

## Verification

-   `yarn validate` -> FAIL
-   Notes: Node v24.13.0; repo:work-log failed because `docs/work/log/2026-02-02.md` was missing while structural changes were present.
-   `yarn validate` -> PASS
-   `yarn validate` -> PASS
-   `yarn build` -> PASS
-   `yarn validate` -> FAIL
-   Notes: Node v24.13.0; app validate failed with TS6305 (domain-tourney dist types missing after clean).
-   `yarn validate` -> FAIL
-   Notes: Node v24.13.0; app validate failed on overlay-container closeAll typing (TS2339).
-   `yarn validate` -> PASS
-   `yarn validate` -> PASS
-   `yarn validate` -> PASS
 -   `yarn validate` -> PASS
-   `yarn validate` -> PASS
-   `yarn validate` -> FAIL
-   Notes: Node v24.13.0; repo:guardrails failed due to multi-line import in apps/rankup-spa/services/api/http-client.ts.
-   `yarn validate` -> PASS
-   `yarn validate` -> PASS
-   `yarn validate` -> PASS
-   `yarn validate` -> PASS
-   `yarn validate` -> FAIL
-   Notes: Node v24.13.0; TS errors for missing override modifiers and type-only imports in packages/common + packages/samba, unused EmptyContext and SwaggerParser typing in packages/api-mock, and unused _sessionNeedsRefresh in packages/platform.
-   `yarn validate` -> PASS
-   `yarn validate` -> PASS
-   `yarn validate` -> PASS
-   `yarn validate` -> FAIL
-   Notes: Node v24.13.0; openapi:check reported generated diff after updating packages/api/openapi.yaml (packages/api/src/generated/openapi.ts needs commit).
-   `yarn validate` -> PASS
-   `yarn validate` -> PASS
-   `yarn validate` -> PASS
-   `yarn validate` -> PASS
-   `yarn validate` -> PASS
-   `yarn validate` -> PASS
-   `yarn validate` -> PASS
-   `yarn validate` -> FAIL
-   Notes: Node v24.13.0; domain-tourney moduleResolution `node` could not resolve `@rankup/platform/**.js` exports (TS2307).
-   `yarn validate` -> PASS
-   `yarn validate` -> PASS
-   `yarn validate` -> PASS
-   `yarn validate` -> FAIL
-   Notes: Node v24.13.0; `@rankup/app@workspace:apps/rankup-spa` not found until `yarn install` refreshed the workspace lockfile.
-   `yarn validate` -> PASS
-   `yarn validate` -> FAIL
-   Notes: Node v24.13.0; repo:guardrails failed (multi-line imports) and TS errors in api-mock/domain-tourney.
-   `yarn validate` -> FAIL
-   Notes: Node v24.13.0; TS errors after OpenAPI type updates (mock/tourney/UI adjustments).
-   `yarn validate` -> PASS
-   `yarn validate` -> PASS
-   `yarn validate` -> PASS
-   `yarn validate` -> PASS
