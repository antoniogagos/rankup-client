# Work log - 2026-02-09

## Summary

-   Followed mandatory entry procedure for active Epic 008 support lanes (`START-HERE`, `CURRENT`, active epic files, latest daily log).
-   Baseline verification failed before `WP-008-33 / PR-002` due to missing daily log file required by `repo:work-log` guardrail.
-   Re-ran baseline after creating today log and confirmed `yarn validate` green before code changes.
-   Executed `WP-008-33 / PR-002` by adding `map<Domain>ProblemToDomainError` wrappers in all SPA API mapper domains with gateways, including compatibility aliases for existing wrapper names.
-   Expanded deterministic error fixtures in `packages/testkit/src/fixtures/dto/errors.dto.ts` to cover canonical and engine-specific codes needed by domain-wide P0 error mapping assertions.
-   Added per-domain P0 mapper suites across `accounts`, `achievements`, `creators`, `engagement`, `media`, `promotions`, `ranked`, `rules`, `scoring`, `sports`, `trustSafety`, and `verified`, while keeping `submissions`/`tournaments` suites in place.
-   Updated required tracking artifacts (`AGENTS.md`, `docs/work/CURRENT.md`, active epic files) to mark `PR-002` complete and move active execution to `PR-003`.
-   Executed `WP-008-33` `PR-003` by expanding composition-root smoke negative coverage to canonical error mapping in `accounts/tournaments/submissions/scoring`, adding `accounts` contract coverage, and enforcing canonical `DomainError` passthrough assertions in `tournaments/submissions/scoring` contract suites.
-   Updated required tracking artifacts (`AGENTS.md`, `docs/work/CURRENT.md`, active epic files, and this daily log) to mark `PR-003` complete and move active execution to `PR-004`.
-   Executed `WP-008-33` `PR-004` by adding strict guardrails in `scripts/test-guardrails-p0.ts` to block generic HTTP `Error("HTTP ...")` throws in `http-client` and to enforce canonical `mapProblemToDomainError` delegation for every `*ProblemToDomainError` mapper export.
-   Wired `repo:guardrails` to run the canonicalization checks as a first-class lane closure guardrail and documented the policy in `docs/engineering/testing-p0.md`.
-   Updated required tracking artifacts (`AGENTS.md`, `docs/work/CURRENT.md`, active epic files, ADR 0069, and this daily log) to mark `PR-004` complete and close `WP-008-33`.
-   Executed `WP-008-32` `PR-005` by removing implicit fallback handler injection from `packages/api-mock/src/core/handlers.ts`, introducing explicit static `notImplemented` coverage for remaining non-admin operations in `packages/api-mock/src/core/not-implemented-handler.ts`, and hardening `scripts/repo-operation-coverage.ts` to fail on forbidden fallback injection patterns.
-   Regenerated global operation coverage diagnostics (`diagnostics/operation-coverage-global-report.json`) showing fallback coverage closure (`handlers explicit/no-fallback: 226/226`) with residual `nonCanonicalFixturePath=32` queued for `WP-008-32` `PR-006`.
-   Updated required tracking artifacts (`AGENTS.md`, `docs/work/CURRENT.md`, active Epic 008 docs, and this daily log) to mark `WP-008-32` `PR-005` complete and move active execution to `PR-006`.
-   Audited engine source paths (`packages/rankup/src/{runtime,algorithms,registry,shared}`) for patch-style typing and confirmed no existing `as any`/`as unknown`/`as never` patch patterns.
-   Added `scripts/repo-engine-type-safety-boundary.ts` and wired it into `scripts/repo-guardrails.ts` to enforce a blocking engine boundary: no `any` and no assertion patching via `as unknown`/`as never`.
-   Refined recent PR-005 typing glue to avoid patch-like assertions in operation coverage scripts and handler wiring while keeping `yarn validate` green.
-   Hardened the same guardrail further so `unknown` is also blocked by default and only allowed in explicit runtime contract allowlists (`runtime/ports` interfaces + approved files).
-   Added explicit JSON model types (`packages/rankup/src/shared/models/json.ts`) and migrated runtime hash/idempotency surfaces to `JsonValue`/`JsonObject`, normalizing optional fingerprint fields (`undefined -> null`) to keep deterministic hashing type-safe.
-   Executed a full production-readiness scrutiny and formalized the resulting P0/P1 hardening backlog as `WP-008-34` (`docs/work/epics/008-engine-production-readiness-hardening.md`) with VSCode-grade acceptance criteria and ordered PR plan.
-   Updated mandatory tracking artifacts (`AGENTS.md`, `docs/work/CURRENT.md`, active Epic 008 file, and this log) to register `WP-008-34` as the next lane after `WP-008-32` `PR-006`.
-   Executed `WP-008-32` `PR-006` by enforcing canonical fixture-path closure (`packages/api-mock/src/fixtures/generated/<operationId>.json`) end-to-end: moved residual non-canonical fixtures, hardened `repo:operation-coverage` + `repo:operation-coverage:report` exactness, and confirmed `withCanonicalFixture=226/226` with `fourWayGlobalPass=226/226`.
-   Marked `WP-008-34` `PR-001` complete from the same evidence set (canonical 4-way global closure) and moved active execution to `WP-008-34` `PR-002`.
-   Updated mandatory tracking artifacts (`AGENTS.md`, `docs/work/CURRENT.md`, `docs/work/epics/008-operation-coverage-4way-global.md`, `docs/work/epics/008-engine-production-readiness-hardening.md`, and this log) to reflect `PR-006` closure and the active `PR-002` focus.
-   Executed `WP-008-34` `PR-002` by removing runtime generic throws from critical paths (`join/clear/upsert` idempotency replay + lifecycle evaluator), introducing canonical `RuntimeProblem` for malformed stored outcomes (`idempotencyOutcomeInvalid`), and propagating taxonomy parity to client mapping (`IdempotencyOutcomeInvalid`).
-   Added dedicated runtime guardrail `scripts/repo-engine-runtime-error-surface.ts`, wired it into `repo:guardrails`, and exposed `yarn repo:engine-runtime-error-surface` for focused enforcement.
-   Added deterministic regression coverage in engine tests for each migrated negative path and a mapper P0 test for `idempotencyOutcomeInvalid`, then moved active execution to `WP-008-34` `PR-003`.
-   Updated mandatory tracking artifacts (`AGENTS.md`, `docs/work/CURRENT.md`, active `WP-008-34` epic file, and this log) to register `PR-002` closure and `PR-003` as the next step.
-   Executed `WP-008-34` `PR-003` by introducing file-backed persistent adapters for runtime critical state in `packages/rankup/src/adapters/persistent/**` (`TournamentRepo`, `SubmissionRepo` with CAS, `IdempotencyPort`, `ProcessedEventRepo`, `ScoringRepo`) while keeping engine core portability boundaries intact.
-   Added deterministic engine coverage in `packages/rankup/test/engine-persistent-adapters.test.ts` to enforce byte-equivalent parity versus in-memory baseline (`idempotency + dedupe + ETag`), persistence across adapter recreation, and concurrent compare-and-set semantics.
-   Updated mandatory tracking artifacts (`AGENTS.md`, `docs/work/CURRENT.md`, active `WP-008-34` epic file, ADR 0062, and this log) to mark `PR-003` complete and move active execution to `PR-004`.
-   Executed `WP-008-34` `PR-004` by replacing in-memory runtime auth/trust-safety stubs with policy-matrix adapters (`InMemoryAuthorizationPort`, `InMemoryTrustSafetyPort`) that emit traceable decisions aligned to ADR 0067.
-   Hardened critical runtime mutation paths (`join`, `upsert`, `clear`, `cancel`) so policy decisions are canonical for `403` mapping (`forbiddenRole`/`notMember`) and removed lifecycle authorization bypass in `CancelTournamentUseCase`.
-   Added actor/audit metadata to critical domain-event payloads (`tournament.member_joined`, `submission.upserted`, `submission.cleared`, `tournament.cancelled`) while preserving request/correlation/causation envelope metadata.
-   Expanded engine runtime tests with explicit negative coverage for authorization-deny + trust-safety blocks and event payload audit assertions across critical mutations.
-   Updated mandatory tracking artifacts (`AGENTS.md`, `docs/work/CURRENT.md`, active `WP-008-34` epic file, and this daily log) to mark `PR-004` complete and move active execution to `PR-005`.
-   Executed `WP-008-34` `PR-005` by adding the versioned release-critical catalog (`diagnostics/release-critical-operations.json`) to the global operation-coverage gates and enforcing blocking `releaseCriticalNotImplemented=0` semantics.
-   Completed release-critical handler closure by implementing explicit `api-mock` handlers for `listTournamentMatchdays`, `getTournamentMatchday`, and `getTournamentMatchdayAvailability`, and by removing those operationIds from the static `notImplemented` catalog.
-   Added non-critical fallback canonicalization coverage with fixture-level status expectation (`packages/api-mock/src/fixtures/generated/getMyPreferences.json`) and a dedicated P0 contract (`packages/rankup/test/__tests__/p0/contracts/notImplementedFallback.contract.test.ts`).
-   Updated mandatory tracking artifacts (`AGENTS.md`, `docs/work/CURRENT.md`, active `WP-008-34` epic file, and this daily log) to mark `PR-005` complete and close `WP-008-34`.
-   Executed `WP-008-34` `PR-006` by moving provider match-status canonicalization into OpenAPI SOT (`components.schemas.MatchStatus.x-rankup-canonical-statuses`), extending OpenAPI generation to emit `packages/api/src/generated/match-status-catalog.{ts,json}`, and replacing hardcoded status mapping in `packages/api-mock/src/core/match-status.ts` with generated-catalog consumption.
-   Removed manual `OperationId` literal union drift in `packages/api-mock/src/core/types.ts` by deriving operation ids from generated OpenAPI operations with compile-time context/response consistency assertions.
-   Added blocking anti-drift guardrail `scripts/repo-openapi-sot-drift.ts`, wired it into `repo:guardrails`, and updated mandatory tracking artifacts (`AGENTS.md`, `docs/work/CURRENT.md`, ADR 0063, active epic file, and this daily log) to close `PR-006`.
-   Opened `WP-008-35` (`docs/work/epics/008-sse-streaming-operational.md`) to harden SSE as a product-grade capability with explicit PR sequencing and strict acceptance criteria.
-   Executed `WP-008-35` `PR-001` by introducing a shared SSE standard client (`packages/base/common/sseClient.ts`) with `connect({ sinceCursor })`, `onEvent`, `disconnect`, and `dispose`, including deterministic dedupe (`eventId`) + ordering (`aggregateVersion`) behavior.
-   Migrated `@rankup/testkit` streaming client (`packages/testkit/src/streaming/fakeEventStreamClient.ts`) to consume the shared SSE standard client and added leak visibility (`openConnections`) in `packages/testkit/src/streaming/fakeSseServer.ts`.
-   Added deterministic SSE operational coverage in engine lane (`packages/rankup/test/engine-sse-client.test.ts`) and expanded existing P0 streaming suite (`packages/rankup/test/__tests__/p0/streaming/eventStream.reconnect.test.ts`) to assert disposal hard-stop and zero open connections after each scenario.
-   Updated structural tracking artifacts for the new lane (`AGENTS.md`, `docs/work/CURRENT.md`, `docs/work/epics/008-sse-streaming-operational.md`, ADR 0070 + ADR index, and this daily log) with active execution moved to `WP-008-35` `PR-002`.

## Verification

-   `yarn validate` -> FAIL
-   Notes: `repo:work-log` blocked because structural changes were present and `docs/work/log/2026-02-09.md` did not exist yet.
-   `yarn validate` -> PASS
-   Notes: baseline confirmation after creating `docs/work/log/2026-02-09.md`.
-   `yarn vitest run -c vitest.p0.config.ts apps/rankup-spa/services/api/{accounts,achievements,creators,engagement,media,promotions,ranked,rules,scoring,sports,trustSafety,verified}/__tests__/p0/mappers/*.test.ts` -> PASS
-   `yarn test:guardrails:p0` -> PASS
-   `yarn test:p0` -> PASS
-   `yarn validate` -> PASS
-   `yarn repo:engine-runtime-error-surface` -> PASS
-   `yarn engine:test` -> PASS
-   `yarn vitest run -c vitest.p0.config.ts apps/rankup-spa/services/api/problem/__tests__/p0/mapProblemToDomainError.mapper.test.ts` -> PASS
-   `yarn repo:guardrails` -> PASS
-   `yarn validate` -> PASS
-   Notes: closure evidence for `WP-008-34` `PR-002` (runtime canonical error surface + dedicated guardrail + taxonomy parity).
-   `yarn validate` -> PASS
-   Notes: final rerun after syncing mandatory tracking artifacts for `WP-008-34` `PR-002` closure (`AGENTS`, `CURRENT`, epic, and daily log).
-   `yarn repo:operation-coverage` -> PASS
-   `yarn repo:operation-coverage:report` -> PASS
-   `yarn api-mock:schema-validate` -> PASS
-   `yarn api-http:schema-validate` -> PASS
-   `yarn validate` -> PASS
-   Notes: closure verification after `WP-008-32` `PR-006` tracking updates and `WP-008-34` `PR-001` status transition.
-   `yarn validate` -> PASS
-   Notes: final rerun after updating all mandatory tracking artifacts (`AGENTS`, `CURRENT`, active epics, and daily log) for the `WP-008-32` `PR-006` closure handoff.
-   `yarn validate` -> PASS
-   Notes: baseline + post-tracking verification while establishing `WP-008-34` production-readiness next steps and acceptance criteria.
-   `yarn validate` -> PASS
-   Notes: final rerun after syncing `CURRENT` + Epic support lanes + new `WP-008-34` plan file.
-   `yarn validate` -> PASS
-   Notes: closure rerun after final same-day verification log update to keep evidence monotonic.
-   `yarn validate` -> PASS
-   Notes: final post-log-update rerun after recording closure verification evidence for the planning update.
-   `yarn validate` -> PASS
-   Notes: final rerun after correcting `CURRENT` active-step reference (`WP-008-32` focus on `PR-006`) and syncing same-day tracking entries.
-   `yarn repo:guardrails` -> PASS
-   `yarn validate` -> FAIL
-   Notes: strict `JsonValue` typing surfaced `undefined` fields in runtime fingerprint payloads (`applyMatchFinished`, `upsertMatchdaySubmission`).
-   `yarn validate` -> FAIL
-   Notes: `repo:ratchet` detected generated `.js` artifacts after the failed run.
-   `yarn clean` -> PASS
-   `yarn validate` -> PASS
-   `yarn validate` -> PASS
-   Notes: final rerun after updating mandatory tracking artifacts for strict `unknown` allowlist enforcement in engine guardrails.
-   `yarn repo:operation-coverage` -> PASS
-   `yarn api-mock:coverage` -> PASS
-   `yarn repo:operation-coverage:report` -> PASS
-   `yarn api-mock:schema-validate` -> PASS
-   `yarn api-http:schema-validate` -> FAIL
-   Notes: concurrent execution with `api-mock:schema-validate` caused mock server port collision (`EADDRINUSE :::4010`).
-   `yarn api-http:schema-validate` -> PASS
-   `yarn validate` -> FAIL
-   Notes: `repo:ratchet` detected generated `.js` artifacts in source paths; cleanup required before final verification.
-   `yarn clean` -> PASS
-   `yarn validate` -> PASS
-   `yarn repo:guardrails` -> PASS
-   `yarn repo:operation-coverage` -> PASS
-   `yarn repo:operation-coverage:report` -> PASS
-   `yarn validate` -> PASS
-   `yarn validate` -> PASS
-   Notes: final rerun after updating mandatory tracking artifacts for engine type-safety guardrail hardening.
-   `yarn validate` -> PASS
-   Notes: final post-log-update rerun for `WP-008-33` `PR-004` closure evidence.
-   `yarn validate` -> PASS
-   Notes: final confirmation rerun after updating all mandatory tracking artifacts for `WP-008-33` `PR-002`.
-   `yarn validate` -> PASS
-   Notes: final post-log-update rerun to keep closure evidence aligned with the latest same-day log edits.
-   `yarn vitest run -c vitest.p0.config.ts packages/rankup/test/__tests__/p0/smoke/problemDetails.smoke.test.ts packages/rankup/test/__tests__/p0/contracts/accounts.contract.test.ts packages/rankup/test/__tests__/p0/contracts/tournaments.contract.test.ts packages/rankup/test/__tests__/p0/contracts/submissions.contract.test.ts packages/rankup/test/__tests__/p0/contracts/scoring.contract.test.ts` -> FAIL
-   Notes: sandbox blocked local mock-server bind (`listen EPERM 0.0.0.0`).
-   `yarn vitest run -c vitest.p0.config.ts packages/rankup/test/__tests__/p0/smoke/problemDetails.smoke.test.ts packages/rankup/test/__tests__/p0/contracts/accounts.contract.test.ts packages/rankup/test/__tests__/p0/contracts/tournaments.contract.test.ts packages/rankup/test/__tests__/p0/contracts/submissions.contract.test.ts packages/rankup/test/__tests__/p0/contracts/scoring.contract.test.ts` -> PASS
-   `yarn test:guardrails:p0` -> PASS
-   `yarn test:p0` -> FAIL
-   Notes: sandbox blocked smoke suites from binding local mock-server port (`listen EPERM 0.0.0.0`).
-   `yarn test:p0` -> PASS
-   `yarn validate` -> FAIL
-   Notes: sandbox blocked `api-mock:schema-validate` local bind (`listen EPERM 0.0.0.0:4010`).
-   `yarn validate` -> PASS
-   `yarn validate` -> PASS
-   Notes: final post-tracking-update rerun for `WP-008-33` `PR-003` closure evidence.
-   `yarn validate` -> PASS
-   Notes: baseline verification before starting `WP-008-33` `PR-004`.
-   `yarn test:guardrails:p0` -> PASS
-   `yarn test:p0` -> PASS
-   `yarn validate` -> PASS
-   `yarn engine:test` -> FAIL
-   Notes: initial `WP-008-34` `PR-003` parity scenario locked submissions too early and asserted raw eventId in dedupe persistence; fixed scenario ordering and dedupe-key assertion.
-   `yarn engine:test` -> PASS
-   `yarn test:p0` -> PASS
-   `yarn validate` -> PASS
-   Notes: closure evidence for `WP-008-34` `PR-003` (persistent adapters + parity/persistence/CAS tests) and post-tracking final verification.
-   `yarn validate` -> PASS
-   Notes: final rerun after syncing all mandatory tracking artifacts for `WP-008-34` `PR-003` closure.
-   `yarn validate` -> PASS
-   Notes: final post-log-update rerun to keep closure evidence monotonic after appending the previous verification entry.
-   `yarn engine:test` -> PASS
-   `yarn test:p0` -> PASS
-   `yarn validate` -> PASS
-   Notes: closure evidence for `WP-008-34` `PR-004` (policy-matrix auth/trust-safety hardening + actor/audit event metadata + negative coverage).
-   `yarn validate` -> PASS
-   Notes: final rerun after syncing mandatory tracking artifacts for `WP-008-34` `PR-004` closure (`AGENTS`, `CURRENT`, active epic, and daily log).
-   `yarn validate` -> FAIL
-   Notes: baseline run for `WP-008-34` `PR-005` surfaced type errors in new matchday handlers (`packages/api-mock/src/core/handlers.ts`) and prevented downstream app typecheck (`TS6305` cascade).
-   `yarn typecheck:api` -> PASS
-   `yarn repo:operation-coverage` -> PASS
-   `yarn repo:operation-coverage:report` -> PASS
-   `yarn vitest run -c vitest.p0.config.ts packages/rankup/test/__tests__/p0/smoke/problemDetails.smoke.test.ts packages/rankup/test/__tests__/p0/contracts/notImplementedFallback.contract.test.ts` -> FAIL
-   Notes: exploratory mixed run exposed existing isolated smoke harness drift (`service lookup undefined` in composition-root smoke); kept smoke assertions unchanged and validated fallback with dedicated contract coverage.
-   `yarn vitest run -c vitest.p0.config.ts packages/rankup/test/__tests__/p0/contracts/notImplementedFallback.contract.test.ts` -> PASS
-   `yarn validate` -> FAIL
-   Notes: `repo:ratchet` blocked generated `.js` artifacts after intermediate typecheck/build outputs.
-   `yarn clean` -> PASS
-   `yarn validate` -> PASS
-   Notes: final closure evidence for `WP-008-34` `PR-005` and post-tracking sync (`AGENTS`, `CURRENT`, active epic, daily log).
-   `yarn validate` -> PASS
-   Notes: final rerun after the last same-day tracking edits to keep closure evidence monotonic for `WP-008-34` `PR-005`.
-   `yarn openapi:ops:gen` -> PASS
-   `yarn repo:openapi-sot-drift` -> PASS
-   `yarn typecheck:api` -> PASS
-   `yarn repo:guardrails` -> PASS
-   `yarn openapi:verify` -> PASS
-   `yarn validate` -> PASS
-   Notes: closure evidence for `WP-008-34` `PR-006` (OpenAPI SOT match-status catalog generation + anti-drift guardrail + operation-id typing hardening).
-   `yarn validate` -> PASS
-   Notes: final rerun after adding `repo:openapi-sot-drift` to AGENTS command inventory, keeping closure evidence monotonic.
-   `yarn validate` -> PASS
-   Notes: final post-log-update rerun to keep evidence monotonic after appending the previous verification entry.
-   `yarn engine:test` -> PASS
-   `yarn vitest run -c vitest.p0.config.ts packages/rankup/test/__tests__/p0/streaming/eventStream.reconnect.test.ts` -> PASS
-   `yarn test:p0` -> PASS
-   `yarn validate` -> PASS
-   Notes: closure evidence for `WP-008-35` `PR-001` (shared SSE client standard + testkit migration + engine/P0 operational streaming coverage).
-   `yarn validate` -> PASS
-   Notes: final rerun after syncing all structural tracking artifacts for `WP-008-35` `PR-001` (`CURRENT`, packet, ADR 0070/index, `AGENTS`, and daily log).
