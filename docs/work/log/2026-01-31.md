# Work log - 2026-01-31

## Summary

-   Added ADR 0021 to enforce single-line named imports and no blank-line separators between import statements.
-   Normalized import formatting across packages/scripts to comply with the new guardrail.
-   Updated repo guardrails to enforce import formatting.
-   Added ADR 0022 and root-level VS Code-grade event/disposable helpers (`event.ts`, `lifecycle.ts`, `browser-event.ts`, `disposableController.ts`).
-   Added ADR 0023 and a guardrail to align Lit `css\`` template indentation; normalized existing styles.
-   Added ADR 0024 for Lit `css\`` autofix + tab size 4, with ESLint rule, editor config updates, and style normalization.
-   Added ADR 0025 to introduce `@rankup/base`, moved event/disposable helpers into common/browser, and removed `IEventBus`.
-   Closed Epic 002 and opened Epic 007 for workspace archetype alignment.
-   Extracted platform into `@rankup/platform`, moved env + session manager implementation, and added ADR 0027.
-   Adopted `@rankup/base` helpers in selected UI consumers (rk-auth-wall, rk-drawer) and closed Epic 007.
-   Added ADR 0026 and migrated ESLint to flat config; added lint wrapper that runs Prettier on `--fix` and aligned import sorting.
-   Added ADR 0029 and enforced Lit `html\`` template indentation + closing backtick alignment via ESLint + guardrails; normalized existing templates.
-   Updated Lit ` css\`` formatting to align closing backticks with the  `css\`` line; lint wrapper now runs Prettier on non-TS files only.
-   Added `docs/state/05-api-requests-observed.md` documenting the observed API request flow and gaps vs VS Code/Hadron patterns.
-   Added ADR 0028 plus `docs/architecture/api-request-flow.md`; expanded Epic 007 with API flow WPs and updated service catalog/CURRENT/AGENTS.
-   Migrated tourney UI components off `IRankupApiClient` to `ITourneyService` and expanded the service surface for matchday/ranking.
-   Retired legacy `packages/app/lib/data-service/*` per ADR 0031 and updated observed-state docs.
-   Deferred API facade/network request service introduction (ADR 0032) and closed Epic 007.
-   Closed Epic 001 verification, updated ROADMAP statuses, and marked Epic 003 blocked pending API spec.
-   Added guardrail blocking UI `IRankupApiClient` usage and removed `apiClient` from AppServices (ADR 0033).
-   Reconciled Epic 006 checklist (mock server) and closed remaining Epic 002 DoR checkboxes.
-   Cleaned docs/spec: formatted BREVIARIO, linked it in SPEC-CONTEXT and README.
-   Added guardrail to forbid UI `fetch()` calls (ADR 0034).
-   Added guardrail to forbid UI `@rankup/api-mock` imports (ADR 0035).
-   Added guardrail to forbid `@rankup/api-mock` imports outside platform wiring (ADR 0036).
-   Removed direct `@rankup/api-mock` dependency and path refs outside platform wiring (platform is the sole consumer).
-   Cleaned docs to remove non-platform `@rankup/api-mock` dependency references (platform-only wiring).
-   Scrubbed historical logs and CURRENT wording to remove non-platform `@rankup/api-mock` references.
-   Normalized remaining docs to clarify platform-only `@rankup/api-mock` wiring and UI-only guardrail scopes.
-   Tightened guardrail docs to consistently refer to UI packages (see `docs/architecture/ui-packages.md`).
-   Added a reusable UI packages definition snippet and linked it from START-HERE and the ADR index.
-   Replaced inline UI packages path definitions with links to the shared snippet.
-   Added UI packages legend comments to guardrail command examples that reference UI scopes.
-   Added UI packages legend comments to command examples that scan `packages/app` broadly.
-   Consolidated repeated UI packages legend lines into single scoped notes for long command lists.
-   Added platform exports for deep imports and copied `env.json.example` into `packages/platform/dist` after workspace builds (ADR 0037).
-   Fixed repo copy script to avoid top-level await under tsx CJS transform.
-   Updated @rankup/api-mock exports to resolve `dist/**` runtime outputs (ADR 0039).
-   Normalized dev-server rewrites to collapse duplicate `node_modules` segments (ADR 0040).

## Verification

-   `yarn validate` -> PASS
-   `yarn validate` -> PASS (post event/disposable system + guardrail update)
-   `yarn validate` -> PASS (post event helper fixes)
-   `yarn validate` -> PASS (post Lit css template guardrail + style alignment)
-   `yarn validate` -> PASS (post Lit css autofix + tabs config)
-   `yarn validate` -> PASS (post Lit css guardrail tweak)
-   `yarn validate` -> PASS (post eslint rule rename + import spacing cleanup)
-   `yarn lint --fix` -> PASS (flat config + prettier wrapper)
-   `yarn validate` -> PASS (post ESLint flat config migration)
-   `yarn lint --fix` -> PASS (post Prettier printWidth update)
-   `yarn validate` -> PASS (post lint/format alignment)
-   `yarn validate` -> PASS (post @rankup/base migration + IEventBus removal)
-   `yarn validate` -> PASS (post @rankup/platform extraction)
-   `yarn validate` -> PASS (post @rankup/base consumer adoption)
-   `yarn validate` -> PASS (post ESLint flat config ADR rename)
-   `yarn validate` -> PASS (post ADR 0026 linkage updates)
-   `yarn validate` -> FAIL (repo:guardrails - Lit html template indentation)
-   Notes: Node v24.13.0; failures in `packages/app/elements/score-bets/rk-sb-bet-match/rk-sb-bet-match.ts`, `packages/app/elements/rk-tourney-header/rk-tourney-header.ts`, `packages/app/elements/rk-match-row/rk-match-row.ts`, `packages/app/pages/tourney/pages/rk-tourney-ranking.ts`.
-   `yarn lint --fix` -> PASS (post Lit html template formatting autofix)
-   `yarn validate` -> PASS (post Lit html template guardrail + autofix)
-   `yarn validate` -> FAIL (post API request flow docs; repo:guardrails - Lit css template closing backticks alignment)
-   Notes: Node v24.13.0; failure in `packages/app/elements/rk-tourney-matchday/rk-tourney-matchday-not-started.ts`.
-   `yarn validate` -> FAIL (repo:guardrails - Lit css template closing backticks alignment)
-   Notes: Node v24.13.0; failure in `packages/app/elements/rk-tourney-matchday/rk-tourney-matchday-not-started.ts`.
-   `yarn validate` -> FAIL (TS6305 - base dist not built for @rankup/base imports in app)
-   Notes: Node v24.13.0; errors in `packages/app/elements/rk-auth-wall/rk-auth-wall.ts` and `packages/app/elements/rk-drawer/rk-drawer.ts` referencing missing `packages/base/dist/**` outputs.
-   `yarn lint --fix` -> PASS (post Lit css closing backtick alignment + lint wrapper scope change)
-   `yarn exec tsc -b packages/base` -> PASS
-   `yarn validate` -> PASS (post base build)
-   `yarn validate` -> FAIL (repo:guardrails - Lit css template closing backticks alignment)
-   Notes: Node v24.13.0; failure in `packages/app/elements/rk-tourney-matchday/rk-tourney-matchday-not-started.ts`.
-   `yarn validate` -> FAIL (platform build - env.json.example module + decorators)
-   Notes: Node v24.13.0; errors in `packages/platform/src/environment/browser/env.ts`, `packages/platform/src/instantiation/common/instantiation-smoke.ts`, `packages/platform/src/tourney/browser/tourneyService.ts`.
-   `yarn validate` -> PASS (post typecheck:workspace + platform tsconfig/definitions update)
-   `yarn validate` -> PASS (post WP-007-05 IRankupApiClient removal in UI)
-   `rg -n "data-service" packages/app` -> PASS (no matches)
    -   UI packages = `packages/app/pages/**`, `packages/app/elements/**`, `packages/samba/**` (see `docs/architecture/ui-packages.md`)
-   `yarn validate` -> PASS (post DataService retirement)
-   `yarn validate` -> PASS (post WP-007-06 docs updates)
-   `yarn validate` -> PASS (post WP-007-07 decision + ADR 0032)
-   `yarn validate` -> PASS (post UI IRankupApiClient guardrail + AppServices cleanup)
-   `yarn validate` -> PASS (post Epic 006/002 checklist reconciliation)
-   `yarn validate` -> PASS (post UI fetch guardrail)
-   `yarn validate` -> PASS (post UI api-mock guardrail)
-   `yarn validate` -> PASS (post api-mock guardrail outside platform wiring)
-   `yarn validate` -> PASS (post api-mock dependency cleanup outside platform)
-   `yarn validate` -> PASS (post Epic 001 verification + ROADMAP update)
-   `yarn validate` -> PASS (post api-mock docs cleanup)
-   `yarn validate` -> PASS (post log/CURRENT api-mock scrub)
-   `yarn validate` -> PASS (post api-mock doc normalization sweep)
-   `yarn validate` -> PASS (post guardrail UI-packages wording sweep)
-   `yarn validate` -> PASS (post UI packages snippet + ADR/START-HERE links)
-   `yarn validate` -> PASS (post UI packages dedupe sweep)
-   `yarn validate` -> PASS (post UI packages legend on command examples)
-   `yarn validate` -> PASS (post UI packages legend for packages/app scans)
-   `yarn validate` -> PASS (post legend consolidation for long lists)
-   `yarn validate` -> FAIL (post platform exports + env.json.example copy)
-   Notes: Node v24.13.0; `scripts/repo-copy-platform-env-example.ts` used top-level await under CJS transform.
-   `yarn validate` -> PASS (post repo-copy-platform-env-example fix)
-   `yarn validate` -> PASS (post api-mock exports mapping to dist)
-   `yarn validate` -> PASS (post dev-server node_modules normalization)
