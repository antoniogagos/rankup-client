# Work log - 2026-01-30

## Summary

-   Attempted baseline verification; `repo:guardrails` still fails due to `tsx` IPC EPERM in this environment (Node v22.17.0).
-   Updated api-mock to use project references to @rankup/api (no `paths` to source).
-   Switched `typecheck:api` to build mode and set `@rankup/api` `types` to dist.
-   Fixed mock responses to align with OpenAPI types (authorize response + competition type).
-   Root scripts now use Yarn workspaces for `start` and `validate` (removed `npm --workspace` usage).
-   Fixed app typecheck failures: environment service path now targets `lib/env`, and DI ctor signatures accept typed constructors.
-   Added `AnyCtor` alias for DI constructor boundary and documented the rationale in DI docs.
-   Re-ran `yarn validate`; still blocked by `tsx` IPC EPERM in this environment.
-   Re-ran `yarn validate` under Node 24.13.0; `tsx` IPC EPERM persists.
-   Updated repo guardrail scripts to run via `node --import tsx` (avoids `tsx` IPC).
-   `yarn validate` now passes; Epic 004 marked Done and CURRENT moved to Epic 005.
-   Added skills index and onboarding references; WP template now records the skill used.
-   Note: creating new skill directories under `.codex/skills/` failed in this environment with EPERM; use existing skills listed in the index or create directories manually.
-   Created new skills for Epic 002 and repo workflow after directories were added.
-   Toolchain maintenance: upgraded web-dev-server deps, added app `tslib`, switched app module resolution to `bundler`, and expanded dev-server CSS rewrites for samba styles.
-   Workspace fix: added app-local TypeScript and routed `start` through `yarn exec tsc` to avoid `tsc` PATH issues.
-   Dev-server config fix: switched `web-dev-server.config.ts` to CommonJS `module.exports` so the config loader applies rootDir, nodeResolve, middleware.
-   Dev-server module fix: rewrote workspace `.ts` module requests to `dist/*.js` when available to avoid TS MIME errors in browser.
-   Dev-server module fix: rewrote `node_modules/@rankup/*/*.ts` requests to workspace `dist/*.js` for symlinked packages.
-   Lit runtime fix: disabled `useDefineForClassFields` in app/samba TS configs to avoid class field shadowing warnings.
-   Api-mock resolution fix: added `module`/`types` entries so node-resolve can load built output.
-   Dev-server module fix: mapped `node_modules/@rankup/*` to `/packages/*` and added `.js` fallback to `dist`.
-   Load-spinner fix: bound property observer callbacks to the host element (prevents `toggleAttribute` errors) and added VS Code-style JSDoc/regions.
-   Epic 006 drafted: ADR 0018, architecture doc, epic doc, roadmap entry, and initial skill added.
-   WP-006-01: added TS-only exports for `@rankup/api-mock` subpaths and `dev:server` script.
-   WP-006-02: extracted mock core registry/handlers and refactored client adapter to dispatch via core.
-   WP-006-07: added anti-leakage guardrail for `@rankup/api-mock/server` imports in UI packages.
-   WP-006-03 spike: added `openapi-backend@5.15.0` and a TS spike script; running under Node v24.13.0 with `node --import tsx` fails with `SyntaxError: Unexpected string` in `async-function/require.mjs`. ADR 0018 amended with fallback stack decision; Epic 006 marked blocked.
-   WP-006-03 fallback spike: validated swagger-parser + ajv + openapi-sampler under `node --import tsx`; removed openapi-backend, renamed spike to openapi-contract, and promoted fallback to default stack. Added openapi-contract server module + CLI with params/query/body validation and response mocking. Wired server to @rankup/api-mock/core handlers (default mock fallback). Added stateful CRUD stores for core entities and switched handlers to use store accessors.
-   WP-006-06: implemented scenario engine (delay/status/auth/reset via headers/query), added core scenario parsing utilities, reset helper, and fixed spike typing for tournament CRUD self-test.
-   Added scenario smoke script under `packages/api-mock/scripts` and workspace script `smoke:scenario`.
-   Dev-server module fix: mapped `/src/*` export paths to `dist/*` for packages using `rootDir: src` (prevents TS MIME errors for TS-only exports like `@rankup/api-mock`).
-   WP-006-08: updated docs/skill/roadmap to close Epic 006 and documented `smoke:scenario`.
-   Documented the mock server smoke script in `docs/engineering/mock-server-guide.md` and referenced it from the docs index.
-   WP-002-00 preflight: baseline `yarn validate` PASS and import audit recorded (no UI imports of api-mock/openapi-fetch/lib/env/src/platform; allowlist stays in platform wiring).
-   WP-002-01 DI primitives: verified existing instantiation primitives + smoke file, marked WP complete and queued composition root next.
-   WP-002-02 composition root: centralized mock/real selection in compositionRoot (removed env fallback from api client factory).
-   WP-002-03 UI bridge: UI now uses `getAppServices()` (removed `apiClient` from `AppContext`), no DI/locator exposure.
-   WP-002-04 ITournamentService: confirmed contract/impl/registration already exist; marked WP complete.
-   WP-002-05 Home slice: already uses `ITournamentService` via `getAppServices()` in `rk-tournament-list`.
-   WP-002-06 ESLint guardrails: verified import restrictions already enforced in root eslintConfig; allowlist limited to platform wiring.
-   WP-002-07 docs/catalog: updated DI guide + service catalog to reflect implemented services and AppServices bridge.
-   WP-002-08 closeout: Epic 002 marked Done (ROADMAP + CURRENT updated) with verification evidence.

## Verification

-   `yarn validate` -> PASS
-   `yarn lint` -> PASS (ESLintRCWarning: eslintrc deprecated; pending flat-config migration)
-   `git ls-files | rg "\\.(js|mjs|cjs)$"` -> PASS
-   Notes: guardrails now run via `node --import tsx`; validate completed without errors. Lint clean after fixing web-dev-server config. Legacy JS config deletions are now reflected in the index.
-   `yarn validate` -> PASS (post dev-server/tsconfig/tslib updates)
-   `yarn validate` -> PASS (post app-local TypeScript + start script exec update)
-   `yarn validate` -> PASS (post WDS config export fix)
-   `yarn validate` -> PASS (post TS-to-dist rewrite)
-   `yarn validate` -> PASS (post node_modules/@rankup TS rewrite)
-   `yarn validate` -> PASS (post useDefineForClassFields + api-mock module metadata)
-   `yarn validate` -> PASS (post node_modules/@rankup JS fallback + load-spinner observer fix)
-   `yarn validate` -> PASS (post Epic 006 docs/skill additions)
-   `yarn validate` -> PASS (post WP-006-01 exports + dev:server script)
-   `yarn validate` -> PASS (post WP-006-02 core extraction)
-   `yarn validate` -> PASS (post WP-006-07 anti-leakage guardrail)
-   `yarn workspace @rankup/api-mock spike:openapi-backend` -> FAIL (Node v24.13.0; `SyntaxError: Unexpected string` in `async-function/require.mjs` when running via `node --import tsx`)
-   `yarn workspace @rankup/api-mock spike:openapi-contract` -> PASS (fallback stack)
-   `yarn workspace @rankup/api-mock spike:openapi-contract` -> PASS (post refinements)
-   `node --import tsx packages/api-mock/src/server/cli.ts` -> PASS (starts server)
-   `yarn validate` -> PASS (post WP-006-03 spike artifacts + docs updates)
-   `yarn validate` -> PASS (post openapi-contract updates)
-   `yarn validate` -> PASS (post openapi-contract fixes)
-   `yarn validate` -> PASS (post openapi-contract refinements)
-   `yarn validate` -> PASS (post openapi-contract doc corrections)
-   `yarn validate` -> PASS (post skill verification update)
-   `yarn validate` -> PASS (post WP-006-03 checklist updates)
-   `yarn validate` -> PASS (post WP-006-04 wiring)
-   `yarn validate` -> PASS (post ADR 0018 accepted + index update)
-   `yarn validate` -> PASS (post WP-006-05 stateful CRUD)
-   `yarn validate` -> PASS (post WP-006-06 scenario engine)
-   `yarn workspace @rankup/api-mock smoke:scenario` -> PASS
-   `yarn validate` -> PASS (post scenario smoke script)
-   `yarn validate` -> PASS (post WP-006-08 docs + skill updates)
-   `yarn validate` -> PASS (post AGENTS + work-log update)
-   `yarn validate` -> PASS (WP-002-00 preflight baseline)
-   `yarn validate` -> PASS (post WP-002-00 tracking updates)
-   `yarn validate` -> PASS (post WP-002-00 log update)
-   `yarn validate` -> PASS (post WP-002-01 tracking updates)
-   `yarn validate` -> PASS (post WP-002-01 log update)
-   `yarn validate` -> PASS (post WP-002-02 composition-root update)
-   `yarn validate` -> PASS (post WP-002-02 log update)
-   `yarn validate` -> PASS (post WP-002-03 UI bridge update)
-   `yarn validate` -> PASS (post WP-002-03 log update)
-   `yarn validate` -> PASS (post WP-002-04 tracking update)
-   `yarn validate` -> PASS (post WP-002-04 log update)
-   `yarn validate` -> PASS (post WP-002-05 tracking update)
-   `yarn validate` -> PASS (post WP-002-05 log update)
-   `yarn lint` -> PASS (WP-002-06 guardrails)
-   `yarn validate` -> PASS (post WP-002-06 tracking update)
-   `yarn lint` -> FAIL (import sort in rk-app/rk-unauthenticated-app; require lint in web-dev-server.config.ts)
-   `yarn lint` -> PASS (post import sort + web-dev-server lint override)
-   `yarn validate` -> PASS (post WP-002-06 log update)
-   `yarn validate` -> PASS (post WP-002-07 docs update)
-   `yarn validate` -> PASS (post WP-002-07 log update)
-   `yarn validate` -> PASS (WP-002-08 closeout)
-   `yarn validate` -> PASS (post WP-002-08 log update)
-   `yarn validate` -> PASS (post dev-server /src export rewrite)
-   `yarn validate` -> PASS (post ProviderService/@service + ADR 0019)
-   `yarn lint` -> PASS (post import sort + provider lint fixes)
-   `yarn validate` -> PASS (post lint fixes for ProviderService/@service)
-   `yarn lint` -> PASS (post @service migration in UI)
-   `yarn validate` -> PASS (post @service migration in UI)
-   `yarn lint` -> PASS (post inline-decorator guardrail + repo-wide normalization)
-   `yarn validate` -> PASS (post inline-decorator guardrail + repo-wide normalization)
-   `yarn lint` -> PASS (post IEventBus/ISessionManager migration)
-   `yarn validate` -> PASS (post IEventBus/ISessionManager migration)

## Update

-   Fixed lint errors in `packages/app/web-dev-server.config.ts` (import order + dot notation).
-   Re-ran `yarn validate` and `yarn lint`; both PASS.
-   Updated the index to drop deleted JS/MJS configs; tracked-file check now passes.
-   Epic 005 can be marked Done.
-   Adjusted WDS config rootDir to include app root + monorepo packages + repo root (enables `/dist` and `/samba` resolution in dev).
-   Re-ran `yarn lint` after WDS config change; PASS (warning about eslintrc deprecation remains).
-   Simplified dev-server asset rewrite (repo root now the sole rootDir); removed unused outside-root prefixes.
-   Added local type declarations for `@web/dev-server` and `@web/dev-server-core` to avoid TS7016 errors from package exports.
-   Upgraded @web/dev-server to 0.4.6 and @web/dev-server-core to 0.7.5; added direct dev dependency on dev-server-core for typed config.
-   Added `tslib` to app dependencies to satisfy runtime helpers in buildless dev.
-   Switched app `moduleResolution` to `bundler` and removed `baseUrl`.
-   Extended dev-server rewrite to resolve `/samba/styles/*.css` from source or dist when present.
-   Re-ran `yarn validate`; PASS.
-   Added `typescript` to app devDependencies and updated `start` to call `yarn exec tsc` so workspace binaries resolve correctly.
-   Re-ran `yarn validate`; PASS.
-   Fixed `yarn -s tsc` usage in app `start` (Yarn CLI syntax error); now uses `yarn exec tsc`.
-   Re-ran `yarn validate`; PASS.
-   Switched WDS config to CommonJS export so config loader applies rootDir, nodeResolve, and middleware (fixes samba CSS + tslib resolution).
-   Re-ran `yarn validate`; PASS.
-   Added WDS rewrite from workspace `.ts` to `dist/*.js` when present (fixes load-spinner TS MIME error).
-   Re-ran `yarn validate`; PASS.
-   Added WDS rewrite from `node_modules/@rankup/*/*.ts` to workspace `dist/*.js`.
-   Re-ran `yarn validate`; PASS.
-   Set `useDefineForClassFields: false` in app/samba tsconfig to avoid Lit class-field shadowing errors.
-   Added `module`/`types` to `@rankup/api-mock` so dev-server resolves the runtime.
-   Re-ran `yarn validate`; PASS.
-   Mapped `node_modules/@rankup/*` to `/packages/*` and added `.js` fallback to `dist` so WDS can serve workspace JS.
-   Fixed `load-spinner` property observer to bind callbacks to the host element and added VS Code-style JSDoc/regions.
-   Re-ran `yarn validate`; PASS.
-   Added TS-only subpath exports for `@rankup/api-mock` (`./core`, `./server`) and `dev:server` script.
-   Re-ran `yarn validate`; PASS.
-   Extracted mock core registry/handlers and updated `createMockRankupApiClient()` to use core.
-   Re-ran `yarn validate`; PASS.
-   Added guardrail to prevent UI imports of `@rankup/api-mock/server` during `yarn validate`.
-   Re-ran `yarn validate`; PASS.
-   Added `openapi-backend` dependency and a spike script under `packages/api-mock/src/server/` to evaluate routing/validation/mocking; spike fails under Node v24.13.0 with `SyntaxError: Unexpected string` in `async-function/require.mjs`.
-   Removed `openapi-backend`, promoted the fallback stack to default, and added `openapi-contract` server module + spike script.
-   Marked ADR 0018 as Accepted and updated the ADR index.
-   Marked Epic 006 blocked and recorded fallback stack decision in ADR 0018; updated CURRENT accordingly.
-   Updated ADR index notes to reflect the ADR 0018 amendment.
-   Added scenario engine to the HTTP mock server (delay/status/auth/reset via headers/query) and updated core helpers/docs.
-   Added `smoke:scenario` script and verified scenario smoke run.
-   Updated Epic 006 status + CURRENT/ROADMAP and documented smoke script in mock-server docs + skill (AGENTS note updated).
-   WP-002-00 import audit:
    -   `rg -n "@rankup/api-mock" packages/platform` -> matches in `packages/platform/src/api/create-rankup-api-client.ts`
    -   `rg -n "openapi-fetch" packages/app packages/samba` -> no matches
    -   UI packages = `packages/app/pages/**`, `packages/app/elements/**`, `packages/samba/**` (see `docs/architecture/ui-packages.md`)
    -   `rg -n "lib/env" packages/app/pages packages/app/elements` -> no matches
    -   `rg -n "src/platform" packages/app/pages packages/app/elements packages/samba` -> no matches
-   Allowlist remains limited to platform wiring; no UI eradication required yet.
-   WP-002-01: DI primitives already present under `packages/platform/src/instantiation/common/` (validated with existing `instantiation-smoke.ts`).
-   WP-002-02: selection of mock vs real now only in `compositionRoot.ts`; `create-rankup-api-client.ts` no longer reads env defaults.
-   WP-002-03: migrated UI usage to `getAppServices()` and removed `apiClient` from `AppContext`.
-   WP-002-04: ITournamentService already implemented and registered (no code changes required).
-   WP-002-05: Home vertical slice already consumes `ITournamentService` in `rk-tournament-list`.
-   WP-002-06: ESLint import restrictions enforced via `no-restricted-imports` and `@typescript-eslint/no-restricted-imports` overrides in root `package.json`.
-   WP-002-06 follow-up: lint fixes applied (import sort in rk-app/rk-unauthenticated-app; allow CommonJS require in web-dev-server config).
-   WP-002-07: updated `docs/architecture/service-catalog.md` and `docs/architecture/di.md` with real service states and AppServices usage.
-   WP-002-08: enabled UI ProviderService/@service (ADR 0019), added instantiation/browser helpers, updated guardrails, and bootstrapped providers in `rk-app` and `rk-unauthenticated-app`.
-   Epic 002 reopened; closeout moved to WP-002-09 and CURRENT/ROADMAP updated accordingly.
-   Fixed lint violations in ProviderService/@service files (import sort + class member spacing); `yarn lint`/`yarn validate` now PASS.
-   Migrated tournament UI components/pages to `@service` for `IRankupApiClient`/`ITournamentService`, updated guardrails to allow `platform/api/common`, and refreshed the DI/provider docs/catalog.
-   Documented UI imports of `platform/api/common` in DI lint expectations.
-   Added ADR 0020 and guardrail to enforce inline decorator formatting; normalized Lit decorators across packages.
-   Created `vscode-component-conventions` skill for future UI edits.
-   Added IEventBus/ISessionManager services, registered them in the composition root, and migrated eventBus/sessionManager usage to `@service`.

## Files touched

-   `packages/api-mock/tsconfig.json`
-   `packages/api/package.json`
-   `package.json`
-   `packages/app/web-dev-server.config.ts`
-   `packages/app/tsconfig.json`
-   `packages/samba/tsconfig.json`
-   `packages/api-mock/package.json`
-   `packages/samba/load-spinner/load-spinner.ts`
-   `packages/api-mock/src/core/types.ts`
-   `packages/api-mock/src/core/handlers.ts`
-   `packages/api-mock/src/core/registry.ts`
-   `packages/api-mock/src/core/index.ts`
-   `packages/api-mock/src/index.ts`
-   `scripts/repo-guardrails.ts`
-   `docs/work/CURRENT.md`
-   `docs/work/log/2026-01-30.md`
-   `packages/api-mock/src/index.ts`
-   `packages/api-mock/src/mock-db.ts`
-   `packages/platform/src/environment/browser/environmentService.ts`
-   `packages/platform/src/instantiation/common/instantiation.ts`
-   `packages/platform/src/instantiation/common/instantiationService.ts`
-   `packages/platform/src/instantiation/common/descriptors.ts`
-   `docs/architecture/di.md`
-   `docs/work/ROADMAP.md`
-   `docs/work/epics/002-service-layering-di-skeleton.md`
-   `docs/work/CURRENT.md`
-   `AGENTS.md`
-   `docs/work/CURRENT.md`
-   `docs/work/ROADMAP.md`
-   `docs/engineering/mock-server-guide.md`
-   `docs/README.md`
-   `docs/work/epics/002-service-layering-di-skeleton.md`
-   `docs/work/CURRENT.md`
-   `docs/work/ROADMAP.md`
-   `packages/platform/src/api/create-rankup-api-client.ts`
-   `packages/app/lib/app-context.ts`
-   `packages/app/rk-app.ts`
-   `packages/app/elements/rk-drawer/rk-drawer.ts`
-   `packages/app/elements/rk-tournament-footer/rk-tournament-footer.ts`
-   `packages/app/elements/rk-tournament-header/rk-tournament-header.ts`
-   `packages/app/pages/tournament/pages/rk-tournament-ranking.ts`
-   `packages/app/pages/tournament/pages/rk-tournament-matchday.ts`
-   `docs/work/epics/002-service-layering-di-skeleton.md`
-   `docs/work/CURRENT.md`
-   `package.json`
-   `packages/app/rk-app.ts`
-   `packages/app/rk-unauthenticated-app.ts`
-   `packages/app/web-dev-server.config.ts`
-   `docs/architecture/service-catalog.md`
-   `docs/architecture/di.md`
-   `docs/work/epics/005-ts-only-and-toolchain-unification.md`
-   `docs/work/epics/004-greenfield-ids-and-imports.md`
-   `.codex/skills/README.md`
-   `.codex/skills/rankup-repo-workflow/SKILL.md`
-   `.codex/skills/rankup-epic-002-di-foundation/SKILL.md`
-   `.codex/skills/rankup-epic-002-composition-root/SKILL.md`
-   `.codex/skills/rankup-epic-002-ui-bridge/SKILL.md`
-   `.codex/skills/rankup-epic-002-guardrails-eslint/SKILL.md`
-   `docs/work/START-HERE.md`
-   `docs/work/wps/_TEMPLATE.md`
-   `package.json`
-   `packages/app/package.json`
-   `packages/app/tsconfig.json`
-   `docs/adr/0005-vnext-baseline.md`
-   `docs/adr/0018-mock-http-server-mode.md`
-   `docs/adr/README.md`
-   `docs/architecture/mock-server.md`
-   `docs/work/epics/002-service-layering-di-skeleton.md`
-   `docs/work/epics/006-mock-http-server.md`
-   `.codex/skills/rankup-mock-http-server/SKILL.md`
-   `yarn.lock`
-   `packages/api-mock/src/server/openapi-contract.ts`
-   `packages/api-mock/src/server/cli.ts`
-   `packages/api-mock/src/server/index.ts`
-   `packages/api-mock/src/server/spike-openapi-contract.ts`
-   `packages/api-mock/src/core/scenario.ts`
-   `packages/api-mock/scripts/smoke-scenario.ts`
-   `docs/work/ROADMAP.md`
-   `packages/platform/src/instantiation/browser/provider.ts`
-   `packages/platform/src/instantiation/browser/providerService.ts`
-   `packages/platform/src/instantiation/browser/elementScopedServices.ts`
-   `packages/app/rk-app.ts`
-   `packages/app/rk-unauthenticated-app.ts`
-   `docs/adr/0019-ui-service-injection-provider.md`
-   `docs/architecture/di.md`
-   `docs/work/CURRENT.md`
-   `AGENTS.md`
-   `docs/work/log/2026-01-30.md`
-   `packages/app/elements/rk-drawer/rk-drawer.ts`
-   `packages/app/elements/rk-tournament-footer/rk-tournament-footer.ts`
-   `packages/app/elements/rk-tournament-header/rk-tournament-header.ts`
-   `packages/app/elements/rk-tournament-list/rk-tournament-list.ts`
-   `packages/app/pages/tournament/pages/rk-tournament-matchday.ts`
-   `packages/app/pages/tournament/pages/rk-tournament-ranking.ts`
-   `packages/platform/src/instantiation/browser/provider.ts`
-   `package.json`
-   `docs/architecture/service-catalog.md`
-   `docs/adr/0019-ui-service-injection-provider.md`
-   `AGENTS.md`
-   `docs/architecture/di.md`
-   `.codex/skills/vscode-component-conventions/SKILL.md`
-   `.codex/skills/README.md`
-   `docs/adr/0020-inline-decorator-guardrail.md`
-   `docs/adr/README.md`
-   `scripts/repo-guardrails.ts`
-   `docs/work/CURRENT.md`
-   `packages/platform/src/events/common/eventBus.ts`
-   `packages/platform/src/events/browser/eventBus.ts`
-   `packages/platform/src/session/common/types.ts`
-   `packages/platform/src/session/common/sessionManager.ts`
-   `packages/platform/src/session/browser/sessionManagerService.ts`
-   `packages/app/managers/session/types.d.ts`
-   `packages/app/managers/session/session-manager.ts`
-   `packages/platform/src/registerPlatformServices.ts`
-   `packages/platform/src/compositionRoot.ts`
-   `packages/platform/src/appServices.ts`
-   `packages/app/lib/app-context.ts`
-   `packages/app/rk-app.ts`
-   `packages/app/rk-unauthenticated-app.ts`
-   `packages/app/elements/rk-drawer/rk-drawer.ts`
-   `packages/app/pages/tournament/pages/rk-tournament-matchday.ts`
-   `docs/architecture/service-catalog.md`
-   `docs/work/CURRENT.md`
-   `docs/work/epics/002-service-layering-di-skeleton.md`
-   `AGENTS.md`
