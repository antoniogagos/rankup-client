# Current work

-   Active epic: `docs/work/epics/008-sse-streaming-operational.md`
-   Status: In Progress (Epic 008 support lane active on `WP-008-35`; `PR-001` completed; Epic 010 closed)
-   Last updated: 2026-02-09

## Summary

Epic 010 is now fully closed and accepted: final strict-acceptance closure evidence is green (`yarn test:p0`, `yarn engine:test`, `yarn validate`) and all `WP-010-F3` acceptance items are complete. Epic 008 remains open as the active support lane for residual ADR-0056 cleanup and boundary hardening.

Execution focus inside Epic 008 moved from the strict P0 parity playbook (`docs/work/epics/008-p0-testing-parity-1-1.md`) into naming-closure hardening and now into global ADR-0056 closure planning (`docs/work/epics/008-operation-coverage-4way-global.md`).
`PR-001` through `PR-007` are completed (baseline/tracking unblock, mapper error-mapping parity, explicit contract passthrough parity for join/upsert idempotency + If-Match, smoke flow parity with `ranking -> results`, dynamic gateway ratchet parity, determinism/budget gating in scripts + CI, and final closure evidence lock), and `WP-008-29` is closed with final acceptance criteria A-E complete.
`WP-008-30` is completed: SPA API gateways now live under `apps/rankup-spa/services/api/tournaments`, legacy singular folder/import naming is blocked via repo guardrails, and service catalog scope naming reflects canonical `tournaments`.
`WP-008-31` is completed: repository-wide canonical naming now uses `tournament`/`tournaments` across code paths, symbols, docs, skills, fixtures, and runtime slices, with legacy shorthand removed from tracked content.
`WP-008-32` is now opened as the active support-lane packet to remove baseline-scoped ADR-0056 enforcement and implement global 4-way `operationId` coverage with hard ratchet rules for newly added operations.
`WP-008-32` `PR-001` is completed: global coverage inventory is now generated in `diagnostics/operation-coverage-global-report.json`, ADR 0056/quality docs formally mark baseline parity as transitional, and the execution lane moved into gate unification.
`WP-008-32` `PR-002` is completed: `yarn validate` now uses a single global `repo:operation-coverage` gate (no baseline scope filter for coverage), ownership diagnostics were globalized, and active execution now moves to `PR-003` (waivers v2 strict metadata + format migration).
`WP-008-32` `PR-003` is completed: waivers now enforce v2 strict metadata (`owner|reason|issue|createdAt|expiresAt|plan|scope|severity`), legacy `expiresOn` is rejected, TTL is bounded by policy, and active execution now moves to `PR-004` (central gateway operation mapping + strict ownership enforcement).
`WP-008-32` `PR-004` is completed: `gatewayOperationMapping` is now centralized and enforced 1:1 against gateway ownership for all `226` non-admin operations (`owners=226/226`, `mapping=226/226`), removing `missingOwner` transitional debt and moving active execution to `PR-005` (explicit handlers without fallback).
`WP-008-32` `PR-005` is completed: `defaultMockHandlers` now uses explicit per-operation wiring only (`implemented + notImplemented`), implicit dynamic fallback injection was removed, `repo:operation-coverage` now blocks forbidden fallback patterns, and active execution moves to `PR-006` (canonical fixture path enforcement).
`WP-008-32` `PR-006` is completed: fixtures are now enforced as canonical (`packages/api-mock/src/fixtures/generated/<operationId>.json`) with strict global coverage/report checks (`withCanonicalFixture=226/226`, `fourWayGlobalPass=226/226`, `nonCanonicalFixturePath=0`), and execution focus moves to production hardening.
Engine hardening follow-up (2026-02-09): `repo:guardrails` now enforces a dedicated engine type-safety boundary (`scripts/repo-engine-type-safety-boundary.ts`) that rejects `any`, restricts `unknown` to an explicit allowlist (runtime port contracts + approved files only), and blocks assertion patching via `as unknown`/`as never` in `packages/rankup/src/{runtime,algorithms,registry,shared}`.
`WP-008-33` is now opened as a parallel support-lane packet for global ProblemDetails/error taxonomy closure (`docs/work/epics/008-problemdetails-domainerror-global.md`).
`WP-008-33` `PR-001` is completed: runtime HTTP non-2xx paths now map to canonical `DomainError` (no generic `Error("HTTP ...")` in `http-client`), taxonomy expanded with engine-aligned kinds, and new P0 mapper/smoke negative coverage was added; active execution moves to `PR-002` (domain-wide error coverage baseline).
`WP-008-33` `PR-002` is completed: all API domains with gateways now expose `map<Domain>ProblemToDomainError` wrappers, P0 mapper error coverage is present across all domains with canonical + specific cases, and status coverage now explicitly includes `401/403/404/409/422/429/5xx`; active execution moves to `PR-003` (smoke + contract negative matrix).
`WP-008-33` `PR-003` is completed: smoke now exercises canonical negative mapping across `accounts`/`tournaments`/`submissions`/`scoring` via composition root, critical contract suites assert canonical error passthrough for the same domains, and active execution moves to `PR-004` (guardrails + closure).
`WP-008-33` `PR-004` is completed: guardrails now block generic HTTP error throws in the SPA API client and enforce canonical ProblemDetails mapper delegation in API mapper exports; the lane is closed with full evidence and focus returns to `WP-008-32` `PR-006`.
Post-scrutiny production hardening planning is now formalized under `WP-008-34` (`docs/work/epics/008-engine-production-readiness-hardening.md`) with explicit P0/P1 closures and VSCode-grade acceptance criteria (4-way canonical closure, runtime canonical errors, persistent adapters, policy hardening, and release-critical `notImplemented` ratchet).
`WP-008-34` `PR-001` is completed: canonical 4-way global closure is now evidenced and enforced in green (`repo:operation-coverage`, report, schema gates, and `yarn validate`), and active execution now moves to `PR-002` (runtime canonical error surface).
`WP-008-34` `PR-002` is completed: runtime generic throws were removed from critical paths, malformed idempotency outcomes now raise canonical `RuntimeProblem` (`idempotencyOutcomeInvalid`), a dedicated runtime-error guardrail is enforced in `repo:guardrails`, and active execution now moves to `PR-003` (persistent adapters + production concurrency semantics).
`WP-008-34` `PR-003` is completed: file-backed production adapters now exist for `TournamentRepo`/`SubmissionRepo` (CAS)/`IdempotencyPort`/`ProcessedEventRepo`/`ScoringRepo` under `packages/rankup/src/adapters/persistent/**`, with deterministic parity and persistence coverage in `packages/rankup/test/engine-persistent-adapters.test.ts`; active execution now moves to `PR-004` (authorization/trust-safety policy hardening).
`WP-008-34` `PR-004` is completed: runtime now uses policy-matrix authorization/trust-safety decisions with traceability (`adr0067.*`) across critical mutations (`join`, `upsert`, `clear`, `cancel`), event payloads include actor/audit metadata, and explicit engine negative coverage now enforces `forbiddenRole`/`notMember` + trust-safety blocking.
`WP-008-34` `PR-005` is completed: release-critical catalog is now versioned and enforced by the global operation-coverage gate (`releaseCriticalNotImplemented=0/39`), new release-critical regressions are ratcheted by default, and fallback canonicalization for non-critical `notImplemented` is covered by fixture status expectations plus a dedicated P0 contract test.
`WP-008-34` `PR-006` is completed: provider match-status canonicalization now derives from OpenAPI SOT (`x-rankup-canonical-statuses`) through generated artifacts (`packages/api/src/generated/match-status-catalog.{ts,json}`), `api-mock` operation ids no longer use manual literal unions, and a blocking `repo:openapi-sot-drift` guardrail now prevents status/type drift regressions.
`WP-008-35` is now opened to operationalize SSE streaming as a product-grade capability (`docs/work/epics/008-sse-streaming-operational.md`), and `PR-001` is completed with a shared client standard in `@rankup/base/common/sseClient.ts`, migrated testkit usage, and deterministic engine/P0 coverage for dedupe, reconnect, ordering, disposal hard-stop, and leak checks.

Epic 009 landing work is paused and moved to backlog by product decision (2026-02-06) until explicitly resumed. `apps/rankup-web` currently ships the delivered landing runtime baseline (ADR 0057), and the Lit Localize explicit `msg(..., { id })` policy rollout remains completed (ADR 0058).

Epic 010 execution completed the full baseline lane, including residual WP-010-08b/09 closure: staff `cancelled` override is now enforced/documented in runtime policies, api-mock tournament core/results/submissions/rankings/live slice now executes through runtime-backed handlers, `engine:test` is part of `yarn validate`, and ADR-0056 waivers remain at `0` with parity baseline enforcement (`diagnostics/parity-baseline-operations.json`) across coverage/ownership/schema/http scripts.
Post-baseline follow-up `WP-010-F1` expanded parity baseline coverage from `30` to `34` operations by adding `listCompetitions`, `listMyDuels`, `createDuel`, and `createDuelRematch`, with contract-valid fixtures and green parity gates.
Post-baseline follow-up `WP-010-F2` expanded parity baseline coverage from `34` to `37` operations by adding `getUserPublicProfile`, `oauthAuthorize`, and `oauthTokenExchange`, with explicit gateway owner mappings, HTTP operationId alias parity, and OAuth authorize query serialization hardened to avoid invalid optional PKCE values.
`WP-010-F3` is complete and accepted: `AC-010-F3-01`, `AC-010-F3-02`, `AC-010-F3-03`, `AC-010-F3-04`, `AC-010-F3-05`, `AC-010-F3-06`, `AC-010-F3-07`, `AC-010-F3-08`, `AC-010-F3-09`, `AC-010-F3-10`, `AC-010-F3-11`, `AC-010-F3-12`, `AC-010-F3-13`, `AC-010-F3-14`, `AC-010-F3-15`, `AC-010-F3-16`, `AC-010-F3-17`, and `AC-010-F3-18` are complete, with final acceptance evidence recorded in Epic 010.
Post-`AC-010-F3-18`, the unified P0 test lane is now in place (`yarn test:p0`) with deterministic fixtures in `@rankup/testkit`, mapper/contract/smoke/streaming suites, dedicated P0 guardrails (`scripts/test-guardrails-p0.ts`), and CI workflow gate (`.github/workflows/test-p0.yml`).
Post-`AC-010-F3-18` stabilization is complete: moved P0 contract/smoke support into `packages/rankup/test/**` with fixed import wiring, hardened smoke service resolution against DI identifier drift in mixed build states, and narrowed `engine:test` to `packages/rankup/test/engine*.test.ts` so `yarn validate` is not coupled to the P0 lane.

Epic 002 is closed (DI skeleton/service layering complete). Epic 007 is closed: `yarn validate` now builds composite workspace deps (`@rankup/base`, `@rankup/platform`, `@rankup/common`, `@rankup/samba`, `@rankup/api`, `@rankup/api-mock`) before the app typecheck (ADR 0030), UI tournament pages consume `ITournamentCoreService`/`ITournamentMatchdaysService`/`ITournamentRankingService` instead of injecting `IRankupApiClient`, the legacy `packages/app/lib/data-service/*` path was retired (ADR 0031), the API facade/network request service decision was deferred (ADR 0032), guardrails now forbid UI SDK injection, UI `fetch()` calls, UI `@rankup/api-mock` imports, and any `@rankup/api` usage outside app `services/api/**` or in domain packages (ADR 0033, ADR 0034, ADR 0035, ADR 0036, ADR 0048). Mock selection now lives in the app composition root with `@rankup/api-mock` allowlisted there and HTTP-only client factories kept in `services/api/**`. Package exports now map `.js` subpaths to TS sources (no dist runtime), and the shared WDS config resolves workspace packages via node-resolve/commonjs/json with outside-root handling for `/assets`, `/docs`, and `/node_modules` plus `__APP_ENV__` injection (optional `--compile-css`). Platform env now reads only `__APP_ENV__` (no json.example imports) with defaults for missing values. The base + platform extraction and API request flow standardization remain complete (ADR 0025, ADR 0027, ADR 0028). ESLint uses flat config with a lint wrapper that runs Prettier on non-TS files during `--fix` (ADR 0026). Epic 006 (HTTP mock server) remains complete: fallback stack validated, server wired to core handlers, stateful CRUD in core store, scenario engine implemented, and smoke script added (`smoke:scenario`). openapi-backend remains removed (runtime incompatibility under Node v24.13.0).

Epic 008 in progress: adopt an Apibase-style app-level domain layout under `packages/rankup/src/domains/*` (ADR 0049), move composition root + AppServices into `apps/rankup-spa`, enforce platform/domain/UI boundaries (ADR 0049 + ADR 0048), add domain DTOs + gateways to prevent SDK leakage (ADR 0048), remove legacy platform/tournament folders, document the internal game-mode registry + ruleset versioning decision (ADR 0044), and migrate the canonical OpenAPI spec to `packages/api/openapi.yaml` with legacy specs removed. OpenAPI is pinned to 3.1.2 for toolchain compatibility (ADR 0045). Follow-up: Problem Details 413/415 responses + `Vary-AcceptLanguage` header were added and spectral rules aligned to `application/problem+json` + `text/event-stream`. The Problem Details error catalog now lives in `packages/api/docs/errors/README.md` with uploads-domain types verified; repo tree snapshots were refreshed. ScorePrediction rules now define the result scope (default final score after extra time, penalties excluded) and match outcomes expose penalty shootout context in the spec, with CreateTournament rulesetConfig examples added for UI/mocks. InstantiationService now tolerates SyncDescriptor across module copies to avoid dev-server service resolution mismatches. Platform is now infra-only: API client wiring moved into app services with product SDK imports removed from `@rankup/platform` (ADR 0046). The build pipeline now follows Apibase: root `tsc -b` + Rollup bundle + clean, with a shared WDS/esbuild config serving TS directly in dev (ADR 0047); Rollup HTML inputs now use `apps/**/*.html`, the rollup tsconfig excludes server/scripts/config with DOM-only types, and the @rankup/api guardrail no longer matches @rankup/api-mock. SPA bootstrap now lives in `apps/rankup-spa/main.ts`, keeping `rk-app`/`rk-unauthenticated-app` view-only and injecting `appServices`/`sessionManager` from the host with listener disposal in the bootstrap; public app context was removed so public pages now consume `ISessionManager` via provider + redirect helper. The tournament domain now uses a Hadron-style capability split (`shared`, `core`, `matchdays`, `members`, `codes`, `invites`) with per-capability `models/`, `contracts/`, `services/`, `validation/`, plus root `analysis/` and `fixtures/`, and updated agent skills; rankings now live in `packages/rankup/src/domains/scoring/ranking`. Engine scaffolding now includes `shared/`, `algorithms/`, `registry/`, `runtime`, plus placeholder domains for accounts/sports/rules/submissions/engagement/verified/ranked/achievements/media/trustSafety/promotions/creators/admin. Tournament contracts now cover previews, matchday navigation, membership, invitation codes, and direct invites aligned to OpenAPI, with gateway + mocks updated. OpenAPI change protocol is now documented and gateways use shared mapping helpers for aligned DTOs. Gateway guardrails now forbid `...api*` spreads and `as Domain.*` assertions.

Heads-Up is now modeled as a tournament format (`formatId=headsUp`) with format-specific config and duel convenience endpoints, keeping game modes focused on scoring rules and enabling ranking payloads to include heads-up scoreboards.

Epic 003 remains blocked until the API architecture spec is delivered.

Maintenance note (2026-02-03): Updated `yarn validate` ordering to preserve project reference outputs through the app typecheck (clean runs after app validate), set app validate to run `tsc -b` + clean for standalone runs, and aligned TS override/type-only import usage in UI/Samba.
Maintenance note (2026-02-03): WDS dev-server configs are now `.mjs` (TS-only exception) so WDS auto-loads them; start scripts are back to `wds --watch`; wireit is listed in app devDependencies; app configs call `createWebDevServerConfig(appRoot)` to avoid cwd-dependent 404s; WDS uses an outside-root depth prefix plus `.ts`/`.json` MIME overrides to prevent module/asset MIME failures; WDS repo-root discovery now uses the workspace package name with `preserveSymlinks=false` plus `nodeResolve.rootDir`/`nodeResolve.jail` and an outside-root depth guard to keep node_modules resolution inside the repo; tournament domain barrels now export types only to avoid duplicate identifier errors under composite builds.
Maintenance note (2026-02-03): Removed the legacy empty `packages/app` folder; the app lives in `apps/rankup-spa`.
Maintenance note (2026-02-03): Added a Rankup Engine domain partitioning proposal under `docs/architecture/rankup-engine-domain-partitioning.md`.
Maintenance note (2026-02-03): Renamed `packages/rankup/src/domains/tournament` to `packages/rankup/src/domains/tournaments` (ADR 0052).
Maintenance note (2026-02-03): Moved rankings into `packages/rankup/src/domains/scoring` (ADR 0053).
Maintenance note (2026-02-03): Scaffolded Rankup Engine layers + domain placeholders (ADR 0054).
Maintenance note (2026-02-03): Implemented accounts domain (auth/me/users/social contracts + gateways + AppServices).
Maintenance note (2026-02-03): Implemented sports domain (catalog/schedule contracts + gateways + AppServices).
Maintenance note (2026-02-03): Implemented rules domain (game modes + rulesets contracts + gateways + AppServices).
Maintenance note (2026-02-03): Implemented submissions domain (scorePrediction contracts + gateways + AppServices).
Maintenance note (2026-02-03): Implemented engagement domain (chat/live/stats contracts + gateways + AppServices) with api-mock parity.
Maintenance note (2026-02-04): Removed placeholder tournament capabilities now owned by engagement/submissions/scoring/algorithms (chat/stats/recaps/updates/submissions/results/analysis).
Maintenance note (2026-02-04): Removed empty root-level tournament folders not in the partitioning proposal (`common/`, `contracts/`, `models/`, `services/`, `mock/`, `tests/`, `validation/`).
Maintenance note (2026-02-04): Split engagement recaps/updates into dedicated capabilities with shared models and updated app wiring.
Maintenance note (2026-02-04): Implemented verified domain (hub/events) with contracts, gateways, services, and api-mock parity.
Maintenance note (2026-02-04): Implemented ranked domain (seasons/leaderboards) with contracts, gateways, services, and api-mock parity.
Maintenance note (2026-02-04): Implemented achievements domain (catalog/grants) with contracts, gateways, services, and api-mock parity.
Maintenance note (2026-02-04): Implemented media domain (uploads/assets) with contracts, gateways, services, and api-mock parity.
Maintenance note (2026-02-04): Implemented promotions domain (campaigns/rewards) with contracts, gateways, services, and api-mock parity.
Maintenance note (2026-02-04): Implemented creators domain (directory/catalog) with contracts, gateways, services, and api-mock parity.
Maintenance note (2026-02-05): Prepared ADR 0056 operation coverage gate prerequisites (default Problem response policy, operations manifest generation, allowlist spec alignment).
Maintenance note (2026-02-05): Implemented ADR 0056 operation coverage gates (mock coverage, gateway ownership, schema validation, HTTP fidelity) with waivers seeding and validate wiring.
Maintenance note (2026-02-06): Implemented the full conversion landing runtime in `apps/rankup-web` (interactive sections, particle canvas, sticky CTA, SEO metadata) with CTA flow aligned to `/es/registro` (ADR 0057).
Maintenance note (2026-02-06): Added global Lit Localize explicit-id guardrail for `msg()` and backfilled existing calls with deterministic IDs via codemod (ADR 0058).
Maintenance note (2026-02-06): Landing V2 game-first redesign implemented (HUD visual language, upgraded GSAP choreography, richer section interactivity, particle-field V2, refreshed metadata/fonts); WP-009-04 remains open pending Lighthouse mobile target.
Maintenance note (2026-02-06): Landing performance hardening (WP-009-04) moved to backlog/paused until explicit resume request.
Maintenance note (2026-02-06): Completed WP-008-28 ADR-0056 waiver burn-down slice (`tournaments.core|submissions|rankings|matchdays|lifecycle|live|results`) with 30 fixtures, explicit gateway owners, waiver cleanup, and initial schema-flaky tracking waivers for non contract-valid mock responses.
Maintenance note (2026-02-06): Completed WP-008-28 schema-flaky follow-up by aligning OpenAPI tournament/results/submission schemas for contract-valid mock responses and removing the targeted 12 `schemaValidationFlaky` waivers.
Maintenance note (2026-02-06): Continued ADR-0056 burn-down by adding 10 explicit core handlers for `tournaments.core` + `tournaments.lifecycle` in `@rankup/api-mock` and removing the matching `missingMockHandler` waivers.
Maintenance note (2026-02-06): Continued ADR-0056 burn-down for `tournaments.submissions|rankings|results|live` by adding contract-valid core handlers + context wiring, extending HTTP operationId fidelity coverage (including SSE), and removing 13 `missingMockHandler` + 9 `httpFidelityMissing` waivers.
Maintenance note (2026-02-06): Continued ADR-0056 burn-down outside the tournaments slice by adding fixtures for remaining non-admin operations and clearing residual `missingMockHandler` + `missingFixture` + `httpFidelityMissing` waivers; residual waiver categories are now `missingOwner` and `schemaValidationFlaky`.
Maintenance note (2026-02-06): Epic 010 started as priority #1 to implement Rankup Engine Baseline v1 with runtime ports/use-cases, deterministic algorithms, OpenAPI semantics hardening, and formal parity gates.
Maintenance note (2026-02-06): Epic 010 baseline core shipped with runtime use-cases + in-memory adapters + deterministic engine tests (`yarn engine:test`), ADR-0056 ratcheted to `WAIVERS_MAX_TOTAL=0`, parity baseline gates activated, and waivers file emptied.
Maintenance note (2026-02-06): Epic 010 residual closure completed (`WP-010-08b` + `WP-010-09`) with enforced staff cancellation override in runtime and runtime-backed api-mock handlers for tournaments core/results/submissions/rankings/live.
Maintenance note (2026-02-06): Post-baseline `WP-010-F1` expanded parity baseline to include duels + competitions operations and fixed `CreateDuelRequest` allOf validation inconsistency in OpenAPI so new baseline operations remain contract-valid in schema/http gates.
Maintenance note (2026-02-06): Epic 010 was re-opened under `WP-010-F3` after strict acceptance audit; closure now requires finishing all pending `FAIL`/`PARTIAL` criteria, with the test-system migration lane explicitly left for the end (Vitest target, pending confirmation).
Maintenance note (2026-02-06): Completed `WP-010-F3` item `AC-010-F3-01` by persisting deterministic 4xx idempotency outcomes in runtime use-cases (`joinTournament`, `upsertMatchdaySubmission`) and replaying the canonical Problem payload on retries with the same key.
Maintenance note (2026-02-06): Completed `WP-010-F3` item `AC-010-F3-02` by wiring `X-Idempotency-Key` for `clearMyMatchdaySubmission` end-to-end (SPA HTTP client header forwarding, api-mock contract context/header mapping, runtime clear use-case idempotency persistence/replay, and deterministic engine tests).
Maintenance note (2026-02-06): Completed `WP-010-F3` item `AC-010-F3-03` by enforcing response-header parity in both parity gates (`api-mock:schema-validate`, `api-http:schema-validate`), adding operation-status header checks (`ETag`, `X-Request-Id`, and response-declared headers), and enabling handler-level response headers in the OpenAPI mock server pipeline.
Maintenance note (2026-02-06): Completed `WP-010-F3` item `AC-010-F3-18` by moving `engine:test` to Vitest (`vitest.engine.config.ts` + deterministic setup), migrating the runtime suite from `node:test`, adding reusable engine test fixtures, and adding guardrails that forbid `node:test`/`node:assert` plus `.only/.skip` in `packages/rankup/test/**`.
Maintenance note (2026-02-06): Completed `WP-010-F3` item `AC-010-F3-05` by hardening join gating to include `finished` status and by aligning join conflict `Problem.code` mapping (`tournamentArchived`, `tournamentCancelled`, `joinClosed`, `tournamentFull`, `tournamentLocked`) with deterministic engine coverage.
Maintenance note (2026-02-06): Completed `WP-010-F3` item `AC-010-F3-06` by adding `joinTournament` + `joinTournamentByInvitationCode` to parity baseline operations, wiring runtime-backed api-mock join handlers/context mapping, and adding explicit join fixtures with HTTP/client idempotency-header parity support.
Maintenance note (2026-02-06): Completed `WP-010-F3` item `AC-010-F3-07` by adding explicit engine coverage for `penalty_shootout` handling under `scorePrediction` (`resultScope=extra_time` default, penalties excluded) in both direct scoring and runtime ranking scenarios.
Maintenance note (2026-02-06): Completed `WP-010-F3` item `AC-010-F3-08` by adding deterministic lock coverage for kickoff/time boundaries (runtime upsert behavior before/at kickoff and `kickoffWithGrace` boundary assertions) so lock outcomes are time-driven, reproducible, and not only static `lockState` checks.
Maintenance note (2026-02-06): Completed `WP-010-F3` item `AC-010-F3-09` by adding an adversarial concurrent join race test that forces overlapping membership writes and verifies exactly one persisted membership/event for the same `tournamentId+userId` pair.
Maintenance note (2026-02-06): Completed `WP-010-F3` item `AC-010-F3-10` by adding provider/canonical drift runtime tests for sports events (unknown canonical `matchId` rejection without state mutation and independent processing for identical scorelines across different canonical matches).
Maintenance note (2026-02-06): Completed `WP-010-F3` item `AC-010-F3-11` by publishing the baseline per-operation Problem Details catalog (`operationId -> status + Problem.code`) in machine-readable diagnostics and quality docs, linked from API error documentation.
Maintenance note (2026-02-06): Completed `WP-010-F3` item `AC-010-F3-12` by propagating runtime event envelope metadata end-to-end (`requestId`, `correlationId`, `causationId`), adding deterministic metadata defaults (`correlationId`/`causationId` fallback policy), and verifying propagation across join/upsert/apply->recompute event flows in engine tests.
Maintenance note (2026-02-06): Completed `WP-010-F3` item `AC-010-F3-13` by introducing an enforceable no-PII runtime logging guardrail (`scripts/repo-engine-no-pii-logging.ts`), wiring it into `repo:guardrails`, and documenting engine logging policy scope under `docs/quality/engine-no-pii-logging-policy.md`.
Maintenance note (2026-02-06): Completed `WP-010-F3` item `AC-010-F3-15` by adding a dedicated engine portability/boundary guardrail (`scripts/repo-engine-portability-boundary.ts`) and wiring it into `repo:guardrails` to block non-portable imports/globals in `packages/rankup/src/{runtime,algorithms,registry,shared}`.
Maintenance note (2026-02-06): Completed `WP-010-F3` item `AC-010-F3-16` by adding the replay/golden determinism suite (`packages/rankup/test/engine-replay.test.ts`) with exact byte-level assertions against `packages/rankup/test/golden/replay-scoreprediction-v1.json`.
Maintenance note (2026-02-06): Completed `WP-010-F3` item `AC-010-F3-17` by adding a dedicated engine concurrency stress suite (`packages/rankup/test/engine-concurrency.test.ts`) and hardening runtime races with atomic processed-event claiming plus conditional submission persistence for `If-Match` upserts.
Maintenance note (2026-02-06): Closed Epic 010 with final acceptance evidence (`yarn test:p0`, `yarn engine:test`, `yarn validate` all PASS) and moved active focus back to Epic 008 support lane.
Maintenance note (2026-02-08): Added `WP-008-29` parity execution planning document (`docs/work/epics/008-p0-testing-parity-1-1.md`) to drive 1:1 closure with the architect P0 recipe.
Maintenance note (2026-02-08): Completed `WP-008-29` `PR-001` (baseline + tracking unblock) and confirmed `yarn validate` passes without `repo:work-log` failure.
Maintenance note (2026-02-08): Completed `WP-008-29` `PR-002` by adding canonical `Problem -> DomainError` mapping (`apps/rankup-spa/services/api/problem/mapProblemToDomainError.ts` + `packages/rankup/src/domains/shared/errors/domainError.ts`), updating tournament/submissions/stats mapper exports, and converting the 9 mapper P0 suites to assert real error kinds.
Maintenance note (2026-02-08): Completed `WP-008-29` `PR-003` by extending tournament-members/submissions domain params (`idempotencyKey`, `ifMatch`), forwarding those semantics through SPA API gateways + HTTP headers (`X-Idempotency-Key`, `If-Match`), and adding explicit passthrough assertions in P0 contract suites.
Maintenance note (2026-02-08): Completed `WP-008-29` `PR-004` by implementing the `scoring/results` capability (contracts/gateway/service + composition-root/app-services wiring) and switching P0 smoke flow #3 from `ranking -> stats` to `ranking -> results` (`getMyMatchdayResults`) with real api-mock-backed composition execution.
Maintenance note (2026-02-08): Completed `WP-008-29` `PR-005` by replacing static mapper-suite checks with dynamic gateway-pattern ratcheting in `scripts/test-guardrails-p0.ts` (baseline waivers + deterministic mapper test naming), preserving `.only/.skip` enforcement, and validating that a newly introduced gateway without a mapper suite fails guardrails.
Maintenance note (2026-02-08): Completed `WP-008-29` `PR-006` by adding deterministic guardrails (`scripts/test-p0-determinism.ts`), runtime budget gating (`scripts/test-p0-budget.ts` with per-layer + total thresholds), wiring CI enforcement in `.github/workflows/test-p0.yml`, and documenting policy/gates in `docs/engineering/testing-p0.md`.
Maintenance note (2026-02-08): Completed `WP-008-29` `PR-007` by locking final closure evidence (`yarn test:guardrails:p0`, `yarn test:p0`, `yarn engine:test`, `yarn validate` all PASS), closing the parity execution plan with A-E acceptance criteria fully checked, and updating all mandatory tracking artifacts.
Maintenance note (2026-02-08): Completed `WP-008-30` by migrating SPA API gateway paths from the legacy singular path to `services/api/tournaments`, updating all code/docs references, adding a no-legacy naming guardrail in `scripts/repo-guardrails.ts`, and syncing service-catalog naming semantics to canonical `tournaments`.
Maintenance note (2026-02-08): Completed `WP-008-31` by applying canonical `tournament`/`tournaments` naming end-to-end (paths, symbols, runtime slices, skills/docs, and fixtures), plus full gate verification (`yarn test:guardrails:p0`, `yarn test:p0`, `yarn engine:test`, `yarn validate`).
Maintenance note (2026-02-08): Added `WP-008-32` execution plan (`docs/work/epics/008-operation-coverage-4way-global.md`) to drive migration from baseline-scoped ADR-0056 gates to global 4-way coverage with hard new-operation ratchet rules.
Maintenance note (2026-02-08): Completed `WP-008-32` `PR-001` by adding `repo:operation-coverage:report`, publishing `diagnostics/operation-coverage-global-report.json`, and recording ADR/quality transition notes for baseline -> global coverage.
Maintenance note (2026-02-08): Completed `WP-008-32` `PR-002` by replacing split coverage gates with `repo:operation-coverage` in validate, removing baseline scope from coverage gating, and globalizing ownership diagnostics while leaving `missingOwner` as explicit non-blocking debt until `PR-004`.
Maintenance note (2026-02-08): Completed `WP-008-32` `PR-003` by hardening waiver schema to v2 (`expiresAt` + `plan` mandatory, legacy `expiresOn` forbidden, TTL bounded, duplicate waiver keys rejected) and aligning allowlist specification + ADR references.
Maintenance note (2026-02-08): Completed `WP-008-32` `PR-004` by introducing central `gatewayOperationMapping` coverage in `apps/rankup-spa/services/api/gateway-mapping.ts`, backfilling missing account ownership declarations, and enforcing strict owner/mapping parity in `repo:operation-coverage` + `gateways:ownership` (no `missingOwner` non-blocking debt remains).
Maintenance note (2026-02-09): Completed `WP-008-32` `PR-005` by removing implicit fallback handler injection from `api-mock` core handlers, introducing explicit static `notImplemented` handler coverage for remaining non-admin operations, and enforcing fallback-pattern rejection in `repo:operation-coverage`.
Maintenance note (2026-02-09): Completed `WP-008-32` `PR-006` by enforcing canonical fixture paths (`packages/api-mock/src/fixtures/generated/<operationId>.json`) as blocking criteria in coverage/report gates, moving residual non-canonical fixtures, and confirming `withCanonicalFixture=226/226` + `fourWayGlobalPass=226/226`.
Maintenance note (2026-02-09): Added engine type-safety guardrail (`scripts/repo-engine-type-safety-boundary.ts`) and wired it into `repo:guardrails` to block `any`, restrict `unknown` to explicit runtime port/interface allowlists, and reject `as unknown`/`as never` assertion patching in engine core paths.
Maintenance note (2026-02-08): Added `WP-008-33` execution plan (`docs/work/epics/008-problemdetails-domainerror-global.md`) to close global ProblemDetails/error taxonomy consistency (runtime canonical mapping, taxonomy parity, per-domain P0 coverage, and smoke negative drift detection).
Maintenance note (2026-02-08): Completed `WP-008-33` `PR-001` by removing generic HTTP throws in `apps/rankup-spa/services/api/http-client.ts`, mapping non-2xx responses through canonical `mapProblemToDomainError`, expanding engine-aligned `DomainErrorKind`, and adding dedicated P0 mapper + smoke negative tests.
Maintenance note (2026-02-09): Completed `WP-008-33` `PR-002` by adding domain-wide `map<Domain>ProblemToDomainError` wrappers in SPA API mappers, extending `@rankup/testkit` error fixtures for canonical/engine-specific cases, and adding per-domain P0 mapper suites so global error coverage includes `401/403/404/409/422/429/5xx`.
Maintenance note (2026-02-09): Completed `WP-008-33` `PR-003` by expanding smoke negative coverage to canonical error assertions for `getMe`, `listMyTournaments`, `getMyMatchdaySubmission`, and `getMyMatchdayResults`, adding P0 accounts contract coverage, and upgrading contracts for `tournaments/submissions/scoring` to assert canonical `DomainError` passthrough.
Maintenance note (2026-02-09): Completed `WP-008-33` `PR-004` by adding P0/repo guardrails that reject generic `Error("HTTP ...")` throws in `http-client`, enforce canonical `mapProblemToDomainError` delegation for `*ProblemToDomainError` mapper exports, and documenting the policy in `docs/engineering/testing-p0.md`.
Maintenance note (2026-02-09): Completed `WP-008-34` `PR-001` by closing canonical 4-way fixture enforcement evidence (`repo:operation-coverage`, `repo:operation-coverage:report`, `api-mock:schema-validate`, `api-http:schema-validate`, `yarn validate`) and moved active execution to `PR-002`.
Maintenance note (2026-02-09): Completed `WP-008-34` `PR-002` by replacing runtime generic throws with canonical `RuntimeProblem` handling (`idempotencyOutcomeInvalid` + `notFound` lifecycle path), adding dedicated guardrail `scripts/repo-engine-runtime-error-surface.ts`, aligning client taxonomy mapping (`IdempotencyOutcomeInvalid`), and validating with `engine:test` + `repo:guardrails` + `yarn validate`.
Maintenance note (2026-02-09): Completed `WP-008-34` `PR-003` by introducing file-backed production adapters for persistent runtime state (`tournaments`, `submissions`, `idempotency`, `processed events`, `scoring snapshots`) and adding deterministic engine parity/persistence/CAS coverage in `packages/rankup/test/engine-persistent-adapters.test.ts`.
Maintenance note (2026-02-09): Completed `WP-008-34` `PR-004` by replacing in-memory auth/trust stubs with ADR-0067 policy-matrix adapters + traceable decisions, enforcing policy-driven `403` outcomes (`forbiddenRole`/`notMember`) in `join|upsert|clear|cancel`, and adding engine runtime coverage for trust-safety blocks + actor/audit event metadata.
Maintenance note (2026-02-09): Completed `WP-008-34` `PR-005` by adding the versioned release-critical catalog (`diagnostics/release-critical-operations.json`), enforcing blocking `releaseCriticalNotImplemented=0` in `repo:operation-coverage`/report, and adding non-critical fallback canonicalization coverage via fixture status expectation (`getMyPreferences`) plus P0 contract test (`notImplementedFallback.contract.test.ts`).

Maintenance note (2026-02-01): Added optional VS Code multi-root workspace file (`rankup-client.code-workspace`) to mirror repo folders and editor settings (ADR 0041).

Maintenance note (2026-01-30): Updated dev-server dependencies, app module resolution, URL rewrites, app-local TypeScript/tsc invocation, WDS config export, TS-to-dist workspace rewrite (including @rankup node_modules symlinks), node_modules/@rankup -> /packages JS fallback, Lit class-field mitigation, api-mock module metadata, and `/src/*` export rewrites to `dist/*` for packages using `rootDir: src` to restore buildless dev and prevent TS MIME errors. See `docs/work/log/2026-01-30.md`.

Planning note (2026-01-30): Epic 006 drafted (HTTP mock server) with ADR 0018, architecture doc, and initial skill; WP-006-01 completed (api-mock TS-only exports + dev:server script); WP-006-02 completed (mock core registry extraction); WP-006-07 completed (anti-leakage guardrail); WP-006-03 completed with fallback stack; WP-006-04 completed (server skeleton wired to core); WP-006-05 completed (stateful CRUD in core store); WP-006-06 completed (scenario engine); WP-006-08 completed (docs + skill final + evidence).

## Invariants

-   Greenfield mode: breaking changes are allowed until production readiness is declared.
-   No legacy/compatibility shims: remove or replace old paths instead of keeping transitional code.
-   Work tracking is inviolable: structural changes require CURRENT + epic + daily log updates.

## Verification

-   `yarn validate`

## Next actions
1. Keep Epic 009 landing work in backlog/paused state until explicit resume request.
2. Monitor Epic 010 regressions only as maintenance (no new baseline scope until explicitly opened).
3. Execute `WP-008-35` `PR-002` to integrate the SSE standard client into real app/domain streaming contracts.
